# Minder — lokale, privacyvriendelijke uitgaventracker (PWA)

## Wat dit is
**Minder** is een single-file PWA voor het bijhouden van uitgaven, budgetten en liquiditeit.
Je importeert **MT940 (ABN AMRO)** en **N26 CSV**; alles wordt **lokaal in de browser** geparsed en opgeslagen. **Niets verlaat het apparaat** — dat privacymodel is de kern.

Naast Minder bestaan de zusterprojecten **Worden** (mentale gezondheid) en **Dragen** (lichamelijke gezondheid). Die horen in hun eigen mappen; verwar hun concepten niet met deze code.

## Bestanden
- `index.html` — de complete app (~8.760 regels, ~660 functies): HTML + inline `<style>` + inline `<script>`. Dit is het product.
- `sw.js` — service worker. `const CACHE = 'minder-v186'` (het actuele nummer staat altijd in `sw.js` zelf). **Network-first** voor de app-pagina (verse versie online, val terug op cache offline), **cache-first** voor iconen, en **cross-origin/PSD2-backend wordt nooit gecachet** (altijd live).
- `manifest.webmanifest` — PWA-manifest (naam "Minder — uitgaventracker", standalone, `start_url` `./index.html`). Bevat een app-shortcut "Koopcheck" → `./index.html?action=buy`.
- `icon-192.png`, `icon-512.png`, `icon-maskable-512.png`, `apple-touch-icon.png` — iconen (staan ook in de SW-`ASSETS`-lijst).
- `Open-banking-koppeling-plan.md` — referentieplan voor een latere live PSD2-bankkoppeling. **Nog niet gebouwd**; de MT940/CSV-import blijft voorlopig de basis.

## Opslag & datamodel (localStorage)
Sleutels: `minder_tx`, `minder_ovr`, `minder_set`, `minder_own`, `minder_accmeta`, `minder_plan`, `minder_view`.
Globale state in `index.html`:
- `TX` — transacties · `OVR` — categorie-overrides per transactie · `OWN` — eigen rekeningen · `ACCMETA` — rekening-metadata/saldi · `SET` — instellingen incl. budgetten · `PLAN` — plandata.
- `CATS` / `FIXED_CATS` — categorie-definities · `MNAMES` / `MFULL` — maandnamen.
Persistentie: `save()` schrijft alle sleutels, `load()` leest ze terug.

## Schermen (tabs)
Navigatie via `go(name)` → toont `#s-<name>`, markeert de nav. Zichtbaar in de onderbalk:
- `dash` → **Home** (overzicht) · `ins` → **Inzichten** · `act` → **Coach** · `maand` → **Maand** · `vooruit` → **Plan**.
Overige (via knoppen bereikbaar): `tx` → Transacties · `vermogen` → Vermogen · `set` → Instellingen.
Laatst bekeken scherm wordt bewaard in `minder_view`.

## Functiekaart (kernankers in index.html)
- **State:** `save()`, `load()`.
- **Import/parsing:** `ingest()` (gedeelde inlees-routine), `parseMT940()`, `parseCSV()`, `splitCSVLine()`, `finalize()`, `categorize()`, `catOf()`, `hash()`.
- **Render per scherm:** `renderDash()`, `renderIns()`, `renderTx()`/`renderTxList()`, `renderSet()`/`renderSetSheet()`, `renderActions()` (Coach), `renderVooruit()`, `renderVermogen()`.
- **Budget:** `effectiveBudgets()`, `plannedBudgets()`, `budgetBand()`, `setCatBudget()`, `suggestBudgets()`, `totals()`, `openPotjesVerdeling()`.
- **Liquiditeit/prognose:** `monthLiquidity()`, `forecastModel()`, `liquidityDaily()`, `recurringSchedule()`, `accountShortfalls()`.
- **Saldi:** `accBalance()`, `totalBalance()`, `totalSaved()`, `n26SavingsAccounts()`.
- **PSD2 (open banking, referentie-stub):** `psd2Cfg()`, `psd2Connect()`, `psd2StartAuth()`, `psd2HandleCallback()`, `psd2IngestSession()`, `psd2Refresh()`, `psd2Disconnect()`.
- **Gedragslaag:** `MECHANISM_SPEC` (`index.html:4245`) — de regels waaronder de coach mag spreken. Vijf keys:
  - `mentalAccounting` (`index.html:4249`) — stilstaand surplus boven de heilige buffer vs. dure schuld; vuurt bij een renteverschil ≥5% en bedrag ≥€50, maar stelt nooit voor de noodbuffer leeg te halen.
  - `lossAversion` (`index.html:4266`) — dosering: hooguit `condities.maxFramesPerDag` loss-frames per dag, nooit gestapeld, en alleen met een verplichte positieve spiegel; verlies als stakes (weken vertraging), nooit als schuld. Een geplande aankoop uit een gevuld potje telt niet als loss.
  - `temporalDiscounting` (`index.html:4283`) — de parkeer-lus sluiten: een geparkeerde aankoop keert in koude staat terug met dezelfde keuze (doen / nog eens parkeren / laten gaan). Na `condities.herhaalParkeerSignaal` (4) keer parkeren wordt het patroon zacht gespiegeld, zonder een beslissing af te dwingen.
  - `defaultEffect` (`index.html:4300`) — ontwerpprincipe (geen signaal): elke default staat zo dat nietsdoen de gezonde keuze is, altijd zichtbaar en in één tik omkeerbaar. Nooit een default die stilletjes geld beweegt of een doel zet; geen dark patterns.
  - `freshStart` (`index.html:4323`) — één rustig vooruitblik-moment bij een nieuwe maand; kijkt vooruit, wrijft de vorige maand nooit in, uitnodigend en makkelijk weg te tikken.

## Service worker & versiebeleid
- Registratie: `index.html` rond regel 6450 — `navigator.serviceWorker.register('sw.js')` + `reg.update()`; bij `controllerchange` volgt een eenmalige `location.reload()` (met `_reloading`-guard).
- **Bij elke release die de cache moet verversen: hoog `CACHE` in `sw.js` op** (`minder-v186` → `minder-v187`, …). De oude cache wordt in `activate` opgeruimd.
- De `v10/v11/v13`-strings boven in `index.html` zijn inline-SVG-icoonversies, **geen** app-versie.

## Syntax-check
Trek het inline `<script>` uit `index.html` en controleer met **`node --check`** vóór commit.

## Werkconventies
- Nederlands, beknopt, direct.
- Privacy-first: geen enkele gebruikersdata mag het apparaat verlaten (behalve bewust via een toekomstige PSD2-backend uit `Open-banking-koppeling-plan.md`).
- Alles zit in `index.html`; hou de inline structuur (HTML/CSS/JS in één bestand) intact.
- Wijzig je de SW-`ASSETS` of cachegedrag, hoog dan `CACHE` op.

## Vaste beslissingen
*(Staande regels, gedestilleerd uit de changelog. Respecteer deze bij nieuwe code; wil je er een omgooien, doe dat bewust en leg het vast. De versietag verwijst naar de volledige onderbouwing in `CHANGELOG.md`.)*

### Wat telt als wat (rekenregels)
- **Netto per categorie** (`v18`-`v28`): een positief bedrag binnen een expense-categorie verlaagt de uitgave. Elke categorie-/uitgave-som is netto. Resterende `amount<0`-filters zijn bewust debit-only (uitschieter-vlag, spaar-/inkomen-detectie, parsing) en mogen niet zomaar netto gemaakt worden.
- **Doorstroomposten zijn geen uitgave** (`v77`): `uitgeleend`/`geleend`/borg/eigen overboeking zijn `internal`-categorieen en vallen daardoor automatisch buiten `totals().spend/income`, veilig-te-besteden, bespaarquote en budgetnaleving. Kwijtschelden is het enige moment waarop het weer een echte uitgave wordt.
- **Vast = herkende herhaling, niet de categorie** (`v55`): een uitgave is vast als het een herkende terugkerende betaling is (incasso of periodieke overboeking, minstens 2x regelmatig). `isFixed()` leunt op `recurringKeys()`, niet op `FIXED_CATS`. `isFixedCat` blijft bewust wel `FIXED_CATS`-gebaseerd voor coach-stuurbaarheid. Let bij wijzigingen op dubbeltelling tussen `fixDue` en recurring potjes.
- **Een overschreden potje reserveert door** (`v111`): `potjeRest(bud, uitgegeven, dim, daysLeft)` is de enige bron voor "wat heeft dit potje nog nodig" — gebruikt door `varPlanRemaining()`, `safeToSpend()` én `openReservedPotjes()`, zodat de drill-down het hoofdgetal niet kan tegenspreken. Binnen budget: het onbestede deel. Overschreden: het **geplande dagtempo** van dat potje maal de resterende dagen, niet nul — anders wordt veilig-te-besteden juist ruimer naarmate je verder over je budget gaat, terwijl het uitgeven doorloopt. Bewust het plan als maat en niet het tempo van déze maand: na één grote aankoop projecteert dat absurd hoog. Aan het eind van de maand is `daysLeft` 0, dus dan reserveert een leeg potje weer niets.
- **Eén route naar een potje-wijziging** (`v112`): elke wijziging aan een **bestaand** potje loopt via de 'volgende maand'-laag (`SET.budgetsNext`), ook die uit `suggestBudgets()` — de lopende maand ligt vast, precies zoals Instellingen belooft. Alleen een **nieuw** potje mag meteen actief worden, en een potje dat een voorstel op nul zet vervalt via `budgetsNext[k]=0` (door `rolloverBudgets()` als verwijdering afgehandeld), niet door het nu te wissen. Schrijf nooit rechtstreeks `SET.budgets={...}` vanuit een bulk-actie: dat is een tweede route met tegengestelde regels.
- **Een schatting uit je historie telt alleen afgeronde maanden** (`v107`, `v113`): `months()` telt de lopende maand altijd mee en die is niet af. Elke functie die een historisch niveau schat — `noodfondsModel()`, `baseIncome()`, `suggestBudgets()` — filtert daarom op `m < nowYMstr()`. Is er nog geen afgeronde maand, dan blijft de lopende de enige bron (`winLopend`/`msLopend`, anders levert een verse import niets op) en zegt de app erbij dat het nog verschuift. Bouw geen nieuwe schatter die de lopende maand stilzwijgend meeneemt: het getal beweegt dan binnen de maand vanzelf omhoog zonder dat er iets veranderd is.
- **Drie drill-downs achter de budgetkaart, elk met een eigen vraag** (`v114`, `v115`): de **ring en titel** openen `openBudgetCompare()` (hoe doe ik het: besteed vs. budget, tempo), **Uitgegeven** opent `openMonthSpend()` (waar ging mijn geld heen) en **Maandbudget** opent `openPotjesVerdeling(m)` (hoe is mijn plan verdeeld). Elk getal opent precies zijn eigen inhoud: het maandbudget toont **alleen deze maand**, de regel "vanaf volgende maand" opent dezelfde functie met `deel='next'` en toont **alleen die maand**. Twee getallen die dezelfde lijst openen is dubbelzinnig — dan weet je niet welk getal je leest. Doorverwijzen mag wel, en heen en terug: een gelabelde voetlink, alleen als er iets klaarstaat. De som van de rijen is per constructie het getal in de kop — `totalBudget()` bij nu, `plannedTotalBudget()` bij volgende maand, nooit een eigen som (`v104`/`v111`). Geen besteding per potje: dat is `openBudgetCompare`. Bewerken blijft `openPotje()` per rij en `setBudget()` via de voetlink: een drill-down is een extra ingang, nooit een tweede editor (`v61`). De **categorieverdeling woont uitsluitend hier**, als eerste chip van `openMonthSpend()` (`v136`): de kaart op de Inzichten-pagina zelf toonde dezelfde `renderCatBreak()`-uitkomst en is verwijderd. Daarmee is de drill-down de enige lezer, dus `renderCatBreak()` rendert altijd open en heeft geen inklap-kop meer — een kop die wel `SET` schrijft maar niets meer stuurt is een dode knop. Zet die kaart niet terug op de pagina: dan staat dezelfde verdeling weer op twee plekken en is niet te zien welke van de twee je leest. Voor een afgeronde maand bestaat de volgende-maand-weergave niet en staat er dat je potjes een instelling van nu zijn — er is geen potjes-historie per maand (`v59`/`v73`).
- **Vast/variabel omzetten heeft één mechanisme en meerdere ingangen** (`v116`): de classificatie leunt op `isFixed()` (`v55`) met twee handmatige overrides — `SET.fixOvr[t.id]` voor één transactie en `SET.fixOvrM[recurKey(t)]` voor alles van die winkel, waarbij de winkel-override de losse overrides van die winkel opruimt. Omzetten loopt altijd via `openFixedVarDrill()` → `fixFlipAsk()` → `fixFlip()`; bouw daar geen tweede route naast. De drill kent twee scopes: **met** een maand-argument hoort hij bij de Uitgegeven-drill van die maand, **zonder** bij het periode-widget op Inzichten — dat widget (`renderFixedVariable()`, met `openSavingsFlowDrill()`) is in `v165` verwijderd omdat het sinds `v160` door niets meer werd gerenderd; de vraag die het beantwoordde staat nu in de vaste-lastendruk op het maandscherm, en de scope-loze aanroep heeft daarmee geen ingang meer. Die scope staat in `window._fvMonth`, dat alleen `openFixedVarDrill()` zelf zet — bewust niet in `_fvTx`, want dat schrijft `renderFixedVariable()` bij elke `renderIns()`, en `fixFlip()` rendert tussendoor. Vast en variabel tellen samen precies op tot de uitgaven van die periode: geen gat, geen overlap (`v104`).
- **Een storting uit je buffer is geen spaarprestatie** (`v117`): `savedNet()` meet wát er naar je spaarrekening ging, niet waar dat geld vandaan kwam — dus je kon over je bestedingslimiet gaan en tóch een groene spaarquote zien, betaald uit je buffer. `spaarDekking(m)` is de enige bron voor dat verschil: `over` = inkomen − uitgaven, `gedekt` = min(gestort, over), `uitBuffer` = max(gestort − over, 0), en `gedekt + uitBuffer` is per constructie precies `gestort`. Bij `uitBuffer > 0` zakt het oordeel van de spaarquote van `good` naar `mid`, heet het "deels uit buffer", toont de tegel het bedrag in plaats van de afstand (die zou "op je doel" zeggen en zichzelf tegenspreken) en noemt `kpiSpaarBrug()` hetzelfde bedrag. Het **cijfer zelf verandert niet**: de spaarquote blijft meten wat er gestort is (`v109`), alleen het oordeel weet nu waar het vandaan kwam. Zet die dekking nooit in `kpiState(ref)`: `kpiBarColor()` kleurt de hele reeks met diezelfde `ref`, terwijl de dekking alleen over de gemeten maand gaat. Een lopende maand krijgt wel de melding maar geen kleuroordeel (`v103`).
- **De naam van een rekening heeft één bron, en die weet waar hij vandaan komt** (`v118`, `v120`, `v121`): `acctNaam(a)` geeft `{naam, bron}` en `acctNiceName(a)` is exact `acctNaam(a).naam`. **Het diagnose-blokje is in `v146` uit Instellingen verwijderd** (samen met `bankDiagRows/Text/Copy/Toggle/HTML`, `DIAG_HERKOMST`, `diagMaand` en `diagDatum`): het was een read-only hulpmiddel voor één naamprobleem en stond daarna permanent in beeld. `bron` houdt één lezer — de hernoem-sheet toont "Nu: <naam> · <bron>" — dus laat dat retourveld staan. Volgorde: **eigen naam** (`SET.acctName`, op account-id) → de naam uit de id bij een bestandsimport (`'N26 Buffer Rust'` → "Buffer Rust") → vaste tabel `ACCT_NAMES` bij N26 → `'Space ··'+last4` → vaste regel bij ABN → `··last4` → label. Sleutel nooit op last4: `acctLast4()` schraapt zonder IBAN de cijfers uit de id, wat voor élk N26-CSV-potje `26` oplevert (uit "N26") — je zou met één naam vier potjes tegelijk hernoemen. Hernoemen loopt via `acctRenameOpen()`/`acctRenameSave()` vanuit de rekeningenlijst én het saldo-overzicht; leeg opslaan zet hem terug op automatisch, en `accLabel()` volgt de eigen naam zodat de hernoeming ook in de filter-chips en transactiedetails doorwerkt. De oude last4-sleutel `SET.acctNames` wordt eenmalig gemigreerd (`migrateAcctNames()`, vlag `acctNameMigrated`) en daarna alleen nog gelezen. **Bekend en nog niet opgelost:** de PSD2-koppeling levert geen potjesnamen — N26 stuurt in `a.name` de rekeninghouder mee, dus `SET.psd2Accounts[a].label` is daar onbruikbaar en de vaste tabel blijft de terugval; drie ABN-rekeningen heten alle drie "Betaalrekening" (constante in de tak); en `accShort()` gaat nog buiten `acctNaam()` om.

- **Een rekening zonder bekend saldo staat niet in Instellingen** (`v146`): `zichtbareRek()` is de enige bron voor beide lijsten in Instellingen — de saldo-invoer én `accountsCard()`. Standaard toont hij alleen `rekMetSaldo()`; het zijn in de praktijk rekeningen die je niet meer gebruikt, en ze vulden allebei de lijsten met regels waar niets aan te doen valt. **Verbergen, nooit verwijderen:** een rekening bestaat zolang er boekingen met die id zijn (`v122`), dus wegnemen zou transacties moeten wissen — een heel andere ingreep, met een eigen bevestiging. Er verandert dan ook **geen enkel cijfer**: `totalBalance()`, `accBalance()`, `totals()` en de onbekend-staten (`v59`/`v73`) lezen onverkort `OWN`, en een test vergelijkt die allemaal voor en na het omschakelen. `legeRekRegel()` zet er één omkeerbare regel onder (`SET.toonLegeRek`), zodat nietsdoen de rustige weergave geeft en één tik alles terugzet (defaultEffect). **De guard telt:** is er geen enkel bekend saldo, dan wordt er niets verborgen en verschijnt er geen regel — anders zou de lijst leeg zijn op precies het moment dat je je eerste saldo moet invullen, terwijl de hero je juist daarheen stuurt.
- **Een rekening is een account-id in je transacties, meer niet** (`v122`): `applyOwnAccounts()` leidt `OWN` rechtstreeks af uit `TX`, dus een rekening bestaat zolang er boekingen met die id zijn — er is geen aparte rekeningenlijst om op te schonen. De twee bronnen bouwen die id verschillend op: de CSV-parser maakt `'N26 ' + de kolom Account Name`, de PSD2-koppeling de cijfers uit de IBAN (`ibanNum`). Hetzelfde potje telt daardoor als twee rekeningen zodra je van import naar koppeling overstapt. Dubbeltelling van boekingen is er niet: `commitTx()` slaat een binnenkomende transactie over als dezelfde soft-key al bestaat op een **andere rekening én uit een andere bron** (`v51`). Het diagnose-blokje toont daarom per rekening de periode (eerste t/m laatste boeking) en de herkomst (koppeling / CSV-import / MT940-bestand), met per bank de vaststelling of bestand en koppeling elkaar overlappen — die toets kijkt op de dag, dus toon daar ook de dag en niet de maand, anders leest "t/m apr, begint apr — geen overlap" als een tegenspraak.
- **Twee ids met dezelfde boekingen is een fout, en de app zegt het** (`v149`): het gat uit `v122` — de CSV-parser bouwt een id als `'N26 <Account Name>'`, de koppeling uit `ibanNum(iban)` — kan hetzelfde potje als twee rekeningen laten bestaan. De soft-dedup in `commitTx()` (`v51`) vangt dat alleen waar beide bronnen elkaar overlappen; gebeurt dat niet, dan staan de boekingen er twee keer én telt `totalBalance()` het saldo dubbel. Dat is een **correctheidsvraag**, geen cosmetica, dus hij wordt gemeld en niet weggelaten. `rekeningOverlap()` vergelijkt de boekingen zelf met `_softKey` — dezelfde sleutel als de dedup, nooit een tweede definitie — en meldt een paar pas bij **minstens 3 gedeelde boekingen én minstens 60% van de kleinste van de twee**; een handvol toevallig gelijke bedragen koppelt anders twee losse rekeningen. De regel staat onder de rekeningenlijst en **alleen bij een vondst**: geen overlap, geen regel, geen lege staat. `rekFeiten(a)` is de enige bron voor zowel de sheet als `rekOverlapTekst()`, zodat wat je leest en wat je kopieert niet uiteen kunnen lopen. **De lijst loopt over `allAccounts()`, niet over `OWN`** (`v152`): een rekening die je bank wél deelt maar waarop nog geen boeking staat valt buiten `OWN` (`v122`) en zou dus onzichtbaar zijn in precies de sheet die je opent omdat je een rekening mist. `rekZonderBoekingen()` benoemt ze apart, en de regel eronder verschijnt óók zonder overlap — anders is de sheet na een samenvoeging niet meer te openen. Alles is **read-only** — een test vergelijkt `TX`, `OWN`, `totalBalance()` en heel `SET` voor en na het openen. Los het samenvoegen nooit stilzwijgend op: welke van de twee ids de echte is, weet alleen de gebruiker. **Twee grenzen, uit echte data geleerd (`v150`):** (1) twee **live** PSD2-rekeningen mogen legitiem dezelfde boekingen hebben — `v51` laat dat bewust toe, want twee Spaces die je identiek vult geven identieke `_softKey`s — dus een vondst tussen twee `psd2`-rekeningen is een vráág, geen vaststelling; wat het beslist is de **uid en de vervaldatum van de consent**, want een uid is per sessie en dezelfde Space uit twee consents krijgt twee ids. (2) De duplicaten van het CSV↔PSD2-type uit `v122` vindt deze toets juist **niet**, en dat is logisch: daar heeft de dedup zijn werk gedaan en zijn de gedeelde boekingen al overgeslagen, dus er is geen overlap meer om te vinden. Zoek die met de naam en de periode, niet met de boekingen.
- **De id van een rekening mag nooit aan de sessie hangen** (`v151`): `psd2IngestSession()` leidde de id van een rekening **zonder IBAN** af uit de `uid`, en die is per sessie — de API zegt het letterlijk: *"valid only until the session to which the account belongs is in the AUTHORIZED status"*. Elke nieuwe machtiging gaf zo'n rekening dus een nieuwe id, een tweede exemplaar met dezelfde boekingen en een **dubbel geteld saldo**; rekeningen mét IBAN hadden er geen last van, want `ibanNum()` is stabiel. De volgorde is nu **IBAN → `identification_hash` → uid**, waarbij die hash precies daarvoor bedoeld is (*"can be used for matching accounts between multiple sessions"*) en in `meta[accId].hash` wordt bewaard. Een rekening waarvan de hash al bekend is **houdt zijn bestaande id**, ook een oude uid-id — anders zou juist deze fix een derde exemplaar maken. De uid blijft alleen als laatste terugval. **Samenvoegen is een aparte, expliciete handeling** (`rekSamenvoegVraag()` → `rekSamenvoeg()`): dubbele boekingen vallen weg op `_softKey` (dezelfde sleutel als de dedup), de rest verhuist mee zodat je geschiedenis compleet blijft, en de `psd2Accounts`-, `manualBal`-, `acctName`- en `savingsAcc`-sporen van de opgeheven id worden opgeruimd. Doel is de rekening met de **nieuwste machtiging**, anders die met de meeste boekingen. Nooit automatisch samenvoegen: welke van de twee de echte is weet alleen de gebruiker, en de bevestiging noemt per stuk wat er gebeurt omdat het onomkeerbaar is.
- **Geen eigen IBAN is af te lezen aan de id** (`v150`): `psd2IngestSession()` doet `accId = ibanNum(iban) || ('psd2_'+uid.slice(0,8))`, dus een id die met `psd2_` begint betekent dat de bank voor die rekening géén IBAN meestuurde — bij N26 is dat het onderscheid tussen een Space zonder eigen IBAN en een rekening met. `rekFeiten()` geeft dat als `eigenIban` en de sheet toont het met zoveel woorden. Leid er niets anders uit af: de naam die N26 meestuurt is de rekeninghouder (`v121`), dus die zegt hier niets.
- **De verdeelregel voor onregelmatig inkomen ligt vooraf vast** (`v155`): `SET.meevallerVrijPct` (standaard `MEEVALLER_VRIJ_DEFAULT`, 10) is het enige wat je instelt; de rest volgt `MEEVALLER_VOLGORDE` — **reserveringen, buffer, aankoopdoel**. Dat is bewust een andere volgorde dan `BELEGGEN_VOORWAARDEN` (buffer eerst): daar is de vraag "wat blokkeert je", hier "waar hoort dit geld als eerste heen". Wil je ze gelijktrekken, doe dat op één plek. De instelling staat in het **zichtbare** deel van Budget & doelen, niet achter Geavanceerd: de regel moet vast te leggen zijn vóór er een meevaller is, en dat is het hele punt. De drempel voor "meevaller" is `MEEVALLER_FACTOR` (1.15) — één constante, gedeeld met de coachlaag; zet er nooit een tweede naast. `meevallerPlan(netto)` geeft `{vrij, posten, rest, volledig}` en verdeelt met `min(nodig, restant)` in die volgorde; is het bedrag kleiner dan de eerste blokkade, dan is er één gedeeltelijke post en `rest:0`. **Twee bedragen bestaan nergens kant-en-klaar en worden omgerekend, niet gemodelleerd:** de buffer in euro's is de omkering van `bufferMaanden()` (`MAAND_DREMPEL.bufferKritiek × essCrisis − spaar`, beide uit `noodfondsModel()`), en het doel is `doelTempo().gat × maandenTot` — `gat` is **per maand**, dus rechtstreeks gebruiken zou een bedrag noemen dat de voorwaarde niet haalt. **Netto, nooit bruto:** uit een transactie is het bedrag per definitie netto; bij handmatige invoer vraagt de sheet expliciet om netto en zegt wat een brutobedrag oplevert. Schat nergens netto uit bruto. **Nooit automatisch verdelen** (`defaultEffect`): vastleggen noteert alleen in `SET.meevallerLog` plus een `coachLogAdd({type:'tip'})` — nooit `afspraak`, anders overschrijft het de maandafspraak (`v139`/`v140`) — en raakt geen saldo, doelbedrag of budget aan. **Zwijgen zodra `beleggenKlaar().volledig` false is**: dan `{vrij:0, posten:[], rest:0}`, zodat een half voorstel niet eens te tonen is. Beleggen komt hier niet in voor, ook niet bij rest.
- **De voorwaarden voor beleggen stellen vast, ze raden niets aan** (`v154`): beleggen hoort met geld dat je op korte termijn niet nodig hebt, en dat is pas zo als drie dingen kloppen — buffer ≥ `MAAND_DREMPEL.bufferKritiek` maanden, dekking ≥ `MAAND_DREMPEL.dekkingOk`%, en een aankoopdoel zonder tekort (`MAAND_DREMPEL.doelOk`). Alle drie staan al als losse regel op het maandscherm, dus `beleggenKlaar(R)` neemt **de rijen van `maandRegels()` als invoer** en roept `dekking()`, `bufferMaanden()` en `doelTempo()` niet opnieuw aan; er wordt niets afgeleid wat het scherm al had. De drempels komen uit dezelfde `MAAND_DREMPEL` als de vijf statussen — voeg er geen tweede constante naast. **Buffer haalt de voorwaarde ook bij status `let op`**: die stand betekent boven de drie maanden maar onder je eigen richtbedrag, en de voorwaarde is de drie maanden; bij dekking en doel is `ok` de enige stand die haalt. **Zwijgen is het gedrag bij onvolledigheid**: ontbreekt een van de drie rijen of staat er `onbekend`, dan `volledig:false` en volgt er **geen** uitspraak — geen 'bijna', geen 'waarschijnlijk nog niet', geen gedeeltelijke uitkomst, hooguit welke bron ontbreekt. `klaar` is per constructie `volledig && alle drie gehaald`, zodat een groen licht nooit op halve data kan staan; een verkeerd groen licht hier kost geld dat binnen een jaar nodig is. Bij niet-klaar noemt de regel **alleen de eerste blokkade** in de volgorde buffer, dekking, doel: één ding tegelijk. De regel herhaalt de cijfers van de vijf regels niet (die staan er al los boven); in de sheet mag de opbouw wel volledig staan. Geen bedrag, geen product, geen percentage, geen aanmoediging bij groen, geen knop die iets in gang zet, geen melding als het omslaat en geen teller. Zichtbaar alleen bij minstens één spaardoel of vermogensinstelling (`beleggenZichtbaar()`).
- **Een bezitting wordt vastgesteld, nooit geschat** (`v157`): boek je inleg naar een beleggingspartij als `sparen`, dan telt hij terecht niet meer als uitgave (`v77`) — maar de app weet daarmee nog steeds niet wat die belegging **waard** is. `SET.assets` is en blijft handmatige invoer. `beleggingZonderBezitting()` stelt alleen vast dat er een partij is waar je structureel geld heen zet zonder bijbehorende bezitting, en noemt **geen bedrag**: uit de stromen valt de waarde niet te reconstrueren, want een opname kan een positie afromen die is opgebouwd vóór je data begint en marktbeweging komt nooit langs je bankrekening. In de praktijkdata liep het netto zelfs negatief (€4.918 in, €8.245 uit), en een saldo kan niet negatief zijn — een schatting zou dus stellig en fout zijn. Buiten de telling vallen: een overboeking waarvan de tegenrekening in `OWN` staat, een partij die de naam van een eigen rekening draagt, een partij die al als bezitting bestaat (ook op deelnaam), en alles onder `BEZIT_MIN_POSTEN` (3) boekingen. Het signaal loopt via de bestaande engine, met de partijnaam in de sleutel zodat snooze en mute per partij werken via groep `bezit`. De tik opent `openAsset(null, naam)`: **alleen de naam** wordt voorgesteld, het bedrag vul je zelf in. `openAsset()` gedraagt zich zonder dat tweede argument exact zoals eerst, en een bestaande bezitting houdt altijd zijn eigen naam. **Bekende grens, dezelfde als `v128`:** `OWN` komt uit `TX` (`v122`), dus een eigen rekening zonder eigen boekingen is niet als eigen te herkennen en kan hier als externe partij opduiken.
- **De Peaks-correctie boekt om, ze verwijdert nooit** (`v156`): de pakketprijs (`PEAKS_PAKKET`, 2,99) is een echte bankkost; elk ander bedrag naar Peaks is inleg. `peaksAudit()` deelt in vier groepen — `pakket`, `inleg`, `opnames` (bijschrijvingen: geld terug uit je belegging) en `onduidelijk` — en `peaksHerkenning()` leidt het patroon uit de data af in plaats van het aan te nemen. **De route is de bestaande categorie `sparen`**, die `type:'internal'` heeft en dus buiten `netSpend()`/`totals().spend` valt (`v77`); een interne overboeking zou vragen dat Peaks in `OWN` staat en dat is niet zo. **Opnames horen mee**: een bijschrijving die in een uitgavecategorie blijft staan valt daar netto weg tegen je uitgaven (`v18`-`v28`) en levert onmogelijke maandtotalen op — in de praktijkdata stond één maand op −€2.022. **Voorzichtig indelen is de regel:** naar `onduidelijk` gaat een post die alleen aan de tegenrekening hangt, en een klein bedrag dat **precies één keer per maand** in drie of meer maanden terugkomt (een prijswijziging). Die tweede eis telt: zonder `keer === maanden` viel een wekelijkse inleg ten onrechte in `onduidelijk`. Corrigeren schrijft per transactie een `OVR`, precies zoals `setCat()`, maar in één bulkactie achter een bevestiging die per groep noemt wat er gebeurt; er verdwijnt geen transactie en er verandert geen bedrag of datum. **`categorize()` leest alleen `t.desc`, en een eigen regel wint van de ingebouwde tabel** — daar staat `sparen` al vóór `bankkosten`, dus een Peaks-post op bankkosten komt per definitie uit een eigen regel. `peaksRegels()` meldt welke regel dat is en biedt aan hem op `sparen` te zetten; zonder dat landt nieuwe inleg opnieuw fout. De automatiseringsregels kennen **geen bedrag**, dus de 2,99-uitzondering past er niet in en blijft handwerk. `peaksSavrulesCheck()` stelt vast dat het bestaande signaal over verkeerd geboekte spaarstortingen hier **niet** op aanslaat: dat zoekt `monthly rule|weekly rule` in `t.name`, en deze posten heten Peaks of Stichting Beheer Derdeng. `coachItems()` blijft onaangeroerd.
- **Vrij besteedbaar per dag is een stand, geen budget** (`v158`): het dagbedrag voor variabele uitgaven was een uitkomst (variabele kosten gedeeld door dagen, achteraf) terwijl die kosten al via potjes lopen; `renderDailyVar()`, `dailyVarSeries()` en de hele `v153`-laag eromheen zijn daarom verwijderd. Wat stuurt is `vrijPerDag()`: `{perDag, ruimte, dagenResterend, volledig}`, met **de ruimte rechtstreeks uit `safeToSpend()`** — nooit een tweede afleiding ernaast. `dagenResterend` klemt op 1 zodat de laatste dag van de maand geen deling door nul geeft, en `volledig` is `S.known>0 && !S.missing`: bij een ontbrekend saldo tonen we niets in plaats van een benadering (`v59`/`v73`), **strenger dan de hero**, die al een getal toont zodra er één saldo bekend is. **Geen instelling**, want een handmatig maandbedrag zou een tweede waarheid zijn die uiteenloopt met `safeToSpend()`. **Geen norm, dus geen kleur, geen doel per dag, geen melding en geen streak**; het bedrag beweegt vanzelf mee als je vandaag niets uitgeeft. `vrijPerDagLine()` is één regel bij het bestaande getal, geen eigen kaart, en herhaalt het bedrag niet omdat dat er groot boven staat; bij een negatieve ruimte staat er dat de ruimte op is en géén negatief dagbedrag. **`SET.dailyVarBudget` is weg**, maar pas nadat het veld op nul stond: toen nam elke leesplek al zijn terugval, dus verwijderen kon per definitie geen uitkomst verschuiven. `dayBudgetNow()` viel terug op `varPool/dim`, precies wat het met een leeg veld al deed; `renderDailyRolling()` en `renderWeekVarKPI()` zijn in `v160` als dode code verwijderd. Een test bewaakt dat een achtergebleven waarde in `SET` niets meer doet. **De streak op een dagdoel is weg** (`v159`): `dayBudgetNow()`, `computeStreak()`, `dayMiniLine()`, `dayStatusLine()`, het `win-streak`-signaal en de wins-regel in `coachMirrorCard()` zijn verwijderd, met hun CSS. Na `v158` was dat dagdoel een afgeleide van je variabele potjes en geen norm meer, en een streak telt dagen onder een grens — die grens bestond niet meer als keuze. Bouw hier geen nieuwe telling op `vrijPerDag()`: dat bedrag beweegt per dag mee, dus een reeks erop meet niets.
- **Kerncijfers staan op twee schermen en meten je eigen systeem** (`v161`): vier cijfers, verdeeld naar tijdshorizon. **Structureel** (spaarquote, vaste-lastendruk) hoort bij het maandscherm, **operationeel** (budgetnaleving, variabele-lastendruk) bij Inzichten; `kpiTegels()` is de gedeelde renderer zodat opmaak en oordeel niet uiteen kunnen lopen, en de 'loopt nog'-behandeling geldt op beide plekken. De **restsaldo-quote is vervallen** (trekt `v108` en `v110` in): die mat wat er op je betaalrekening bleef staan, en dat is bij een automatische verdeling per definitie hoog — een artefact van je eigen structuur, geen cijfer. **De spaarquote telt de hele vermogensopbouw** via `vermogensInleg(m)`: `savedNet()` voor je spaarrekening, `resThisMonth()` voor de reserveringenpot en `beleggingsInleg()` voor wat naar een externe partij ging. `resThisMonth()` spiegelt `savedThisMonth()` exact — zelfde vorm, zelfde klem op nul, zelfde afronding, alleen op `SET.resAcc`; dubbeltelling is structureel uitgesloten omdat `v128` die rekening uit `isSavingsAcc()` houdt, en `beleggingsInleg()` slaat boekingen over waarvan de tegenrekening in `OWN` zit. **Het aankoopdoel en de aflossing vallen er bewust buiten**: een doel heeft geen rekening en geen transacties (`v99`), en van een leningtermijn kan de app het aflossingsdeel niet van de rente scheiden (`v100`). Schat die nooit alsnog; de uitleg-sheet noemt ze met de reden, zodat een ontbrekende stroom niet als fout leest. **50/30/20 stuurt de banden niet meer**: alleen budgetnaleving houdt een doel, want dat meet tegen je eigen potjes. **De norm zelf is in `v165` vervallen**: hij bleef staan voor `ruleOfThumbCard()`, en die had toen al geen aanroeper — daarmee stuurde `SET.splitTarget` niets meer en was het instellingenpaneel de enige lezer van zijn eigen instelling. De hele laag (`SPLIT_FRAMES`, `splitTarget()`, `setSplitNorm()`, `healthSplit()`, `ruleOfThumbCard()`) is weg; een achtergebleven `SET.splitTarget` doet niets, en een test bewaakt dat. Waar geen eigen voorwaarde is, staat er geen doel — een cijfer zonder norm is bruikbaar, een cijfer met de verkeerde norm is misleidend. **`kpiBasis(key,t)` staat los van de band**, want `kpiBijdrage()` leunt op de noemer en die bestaat voor alle vier. **`MAAND_DREMPEL.kpiMinBedrag`**: onder dat grondtal toont een cijfer het bedrag in plaats van een percentage — een terugstorting in een categorie met nauwelijks kosten gaf anders een vaste-lastendruk van −1%.
- **De spaarquote telt elke euro één keer, en de lijst erachter bewijst het** (`v163`): `vermogensInleg()` telt drie stromen op, en de tweede kant van een overboeking naar je eigen spaarpot landde in de derde. Herkent je bank de tegenrekening, dan vangt de `refNums`-toets dat af (`v128`); levert hij die niet, dan is de **spiegelboeking** het enige bewijs dat er is. `vermogensPotten()` (spaarrekeningen plus `SET.resAcc`) en `inlegSpiegels()` (bijschrijvingen op die potten, per bedrag) voeden `beleggingsTx()`, dat een afschrijving overslaat zodra hij al als instroom geteld is, van een pot zelf komt, een herkende eigen tegenrekening heeft, of een spiegel van hetzelfde bedrag in dezelfde maand tegenover zich vindt. **Elke spiegel wordt hooguit één keer gebruikt**, anders zou één bijschrijving twee afschrijvingen wegstrepen. `beleggingsTx()` is de enige bron voor zowel het bedrag als de lijst, dus die twee kunnen niet uiteen lopen. `kpiTx('inleg')` geeft sinds nu `inlegTx()`: de vereniging van de drie stromen, ontdubbeld op id, want een reserveringspot mag ook als spaarrekening gemarkeerd staan. De waarde per boeking staat in `inlegWaarde(t)` en volgt de **stroom**, niet een globale vlag: op een eigen pot telt de bijschrijving, daarbuiten de afschrijving. Nul beleggingsboekingen is geen nul ingelegd maar **onbekend** (`v59`/`v73`), dus dat deel is `null` en niet `0` — anders kon `totaal` nooit meer onbekend worden. `insKpiSeries()` leest dezelfde `vermogensInleg()`, want een sparkline die alleen de spaarrekening telt spreekt zijn eigen tegel tegen. Wat er **niet** in zit en dat ook zegt in de uitleg-sheet: een aflossing en een inleg in een doel dat je zelf bijhoudt zijn niet uit je boekingen af te leiden.
- **De brug hangt aan `spaarDekking()`, niet aan een tweede cijfer** (`v163`): `kpiSpaarBrug()` vergeleek de restsaldo-quote met de spaarquote en gaf sinds `v161` stilzwijgend `''` terug, want `insKpis().spaar` bestond niet meer. De vraag eronder staat nog overeind, dus de zin komt nu rechtstreeks uit `spaarDekking()` en noemt euro's in plaats van twee percentages. Het cijfer wordt er **niet** op bijgesteld — `v161` trok die bijstelling in, de spaarquote meet wat er opzij ging en niet waar het vandaan kwam — de sheet zegt het er alleen bij. Opgeruimd wat met de restsaldo-quote meeging: `KPI_META.spaar`, de `spaar`-takken in `kpiState()`, `kpiTx()`, `kpiTxLijst()` en `kpiAfstandTxt()`, de reeks `S.spaar`, en `dismissInlegNoot()` met de introductienoot die al leeg stond.
- **Dode code wordt verwijderd, en bereikbaarheid is de toets** (`v164`, `v165`): 165 functies hadden geen aanroeper meer — samen zo'n 1.650 regels, ruim een zesde van `index.html`. Het waren de resten van vijf opgeheven lagen: het oude dashboard (`renderDash_OLD_UNUSED` ging in `v160`, zijn twintig widgets bleven staan), de vooruitblik-zone van `v70`/`v80`, het coachscherm van `v143`, de kerncijfers van `v161` en de referentie-verdeling. **Drie meetfouten om niet te herhalen.** (1) Commentaar telt niet als verwijzing: een naam die alleen nog in een comment staat hield `vooruitDeep()`, `renderUpcoming()`, `renderFixedVariable()` en `ruleOfThumbCard()` ten onrechte levend, vaak in een comment die juist vertelde dát ze weg waren. (2) Meet iteratief: een dode functie houdt zijn eigen hulpfuncties levend, dus na elke ronde ontstaan er nieuwe. (3) **Tellen is niet genoeg** — een groep dode functies die naar elkaar verwijst houdt zichzelf levend en heeft altijd twee of meer verwijzingen. Zo bleef de hele norm-laag staan, en `yearReview` met `openYearReview`. De toets is daarom **bereikbaarheid**: loop vanaf de echte startpunten (de HTML buiten het script, plus de boot-code buiten elke functie) de aanroepgraaf af, en wat je niet tegenkomt is dood. Er is **geen uitzonderingslijst** meer: een functie die niemand kan bereiken is geen voorraad maar ballast, en een vastgelegde beslissing die zegt dat iets blijft bestaan is geen bewijs dat het aan staat — controleer dat apart.
- **Er is maar één maand, en dat maakt elke `!isNow`-tak dood** (`v166`): `months()` voegt de huidige maand altijd toe en sorteert oplopend, `render()` zet `curMonth` op het laatste element, en er is **geen maandkiezer** — `syncMonthPick()` stuurde een `#monthPick` aan dat niet in het bestand staat. `curMonth` is in de draaiende app dus altijd de lopende maand. Daarmee waren de `!isNow`-takken in `renderIns()`, `renderVooruit()` en `renderActions()` onbereikbaar, met `heroProjection()`, `renderSavings()` en de niet-bare tak van `renderPlan()` eraan vast. Ook onbereikbaar: de lege staat bovenin `renderIns()`, `renderVooruit()` en `renderDash()`, want `render()` valt bij nul transacties al terug op `renderEmpty()`, dat die drie schermen zelf vult. Bouw hier geen nieuwe `!isNow`-tak: komt er ooit een maandkiezer, dan is dat een ontwerpbeslissing met een eigen ronde, niet een tak die stilzwijgend meelift. Let op wat er **wel** blijft: `openPotjesVerdeling()` en `scoreNotifs()` krijgen hun maand als argument en hebben hun `isNow` terecht.
- **Bij nul data claimt geen enkel scherm iets** (`v166`, `v167`): drie dingen zaten onder één symptoom — Maand meldde 'Alle vijf de regels staan goed' terwijl er nul regels waren. (1) `maandRegels()` duwde regel `patroon` **onvoorwaardelijk**, ook zonder een enkele boeking; dan is 'er springt deze maand geen uitgavenpatroon uit' geen vaststelling maar een lege claim, en hield die ene regel de hele samenvatting overeind. Hij staat nu achter `TX.length`, dus met data verandert er niets. (2) `renderEmpty()` vulde een **handmatige lijst** schermen waar `s-maand` en `s-fire` niet in stonden; die lijst komt nu uit de DOM (elk `.screen` behalve `s-set`, dat `renderSet()` doet), zodat een scherm dat later bijkomt niet opnieuw door het gat valt. (3) `go()` rendeerde daarna alsnog over die lege staat heen, want hij riep `renderActions()` en `renderMaand()` rechtstreeks aan. **`geenData()` is de enige bron** en wordt door `render()` én `go()` gelezen; los van elkaar is elke helft zinloos. Los dit nooit op met een uitzondering in één render-functie: dan komt hetzelfde bij het volgende scherm terug. De terugval `!R.length` in `renderMaand()` staat er weer, want `maandRegels()` kan nu leeg teruggeven. Een neveneffect dat klopt: zonder maandscherm wordt `SET.maandGelezen` ook niet meer gezet.
- **Het oordeel telt wat er staat** (`v167`): `maandOordeel()` besloot met de hardgecodeerde zin 'Alle vijf de regels staan goed', terwijl welke regels er staan afhangt van wat je hebt ingesteld — bij drie regels was de zin dus ook bij normaal gebruik onwaar. Hij noemt nu `ok.length`, en **'alle' mag alleen als `ok.length === R.length`**: staat er ook iets op `onbekend`, dan wordt het 'De 2 regels die te toetsen zijn staan goed', anders leest een volledig oordeel over een onvolledige lijst. Eén regel geeft 'De enige regel die te toetsen is staat goed', en een lege lijst 'Er is nog niets te beoordelen' in plaats van een geruststelling over niets.
- **Berekenen en weggooien is ook dode code** (`v166`): `monthVsPrevInner()` bouwde een `.html` met het verschil met vorige maand, een streak-regel en een zin over de grootste veroorzakers, terwijl `whatStandsOutLine()` alleen `.drivers` leest; de streak-lus liep daarvoor `steerSpendUpTo()` over elke maand opnieuw. `insSignals()` gaf per signaal `hyp` (de duiding), `imp` (het dus-wat), `chip` en `tone` mee, en geen daarvan werd ooit gerenderd. Beide zijn tot hun gebruikte output teruggebracht; de getallen zelf blijven, want die staan in `kpiLabel`, `kpiVal` en `kpiSub`. Twee dingen zijn bewust bewaard omdat ze nog getoond kunnen gaan worden (`v167`): `monthVsPrevInner()` geeft `delta` en `partial` terug — een aftrekking van twee waarden die er toch al zijn — met het sjabloon voor het kopgetal als comment ernaast, en de vier `hyp`/`imp`-zinnen staan als comment boven `insSignals()`. De streak-lus komt **niet** terug: die draaide `steerSpendUpTo()` over elke maand opnieuw. Zoek zulke gevallen niet met bereikbaarheid — de functie leeft, alleen een veld van zijn uitkomst niet.
- **De gelezen-vlag bepaalt nooit wat er op het scherm staat** (`v168`): `maandStructureel()` gaf niets meer terug zodra `SET.maandGelezen` vandaag was, en `go('maand')` zette die vlag zelf direct na de eerste render. Je opende Maand, las 'Er is deze maand één ding dat een beslissing vraagt', kwam terug en las 'Er is niets dat vastloopt' — zonder dat er iets was gebeurd. Een scherm dat zichzelf binnen een dag tegenspreekt is erger dan een scherm dat zwijgt. **Een gezien-vlag mag hooguit sturen of je een herinnering krijgt om iets te openen, nooit wat je dan ziet.** Een tweede plek was de onderdrukking ook niet nodig: structurele signalen staan per `v162` al buiten `notifList()`, want `NOTIF_HORIZON` laat alleen `direct` en `correctie` door, dus er viel niets te onderdrukken. `SET.maandGelezen` stuurt nu nog precies één ding aan: de regel 'Gelezen op'. Die wordt daarom **vóór** `renderMaand()` gezet in plaats van erna — anders toont hij de datum van de vorige keer — en dat kan veilig, juist omdat de vlag geen inhoud meer raakt. Een test eist dat drie opeenvolgende openingen op dezelfde dag letterlijk dezelfde inhoud geven, met de gelezen-regel eruit gefilterd omdat die per definitie wel mag veranderen.
- **Zonder aangewezen spaarrekening is je buffer onbekend, niet je banksaldo** (`v168`): `spaarSaldo()` viel terug op `totalBalance()` zodra `totalSaved().n` nul was. Home zei dan 'Je noodfonds staat op €4.000' over een betaalrekening, en dat cijfer voedde `noodfondsModel().spaar` → `bufferMaanden()` → de bufferregel op Maand → `beleggenKlaar()`. Een verzonnen zekerheid op precies dat getal is de duurste die deze app kan hebben: een groen licht om te beleggen terwijl je geen buffer hebt. De terugval is **weg en niet vervangen door een tweede schatting**. `SET.extraSavings` telt wel, want dat is jouw eigen invoer voor potjes die de app niet ziet, dezelfde klasse als een handmatig saldo; `bron` is dan `'extra'` in plaats van `'geen'`. Wat er bij onbekend gebeurt, per lezer: `noodfondsModel().spaar` en `.pct` worden `null` (die guard bestond al), `bufferMaanden()` geeft `null`, de bufferregel **valt weg** van het maandscherm (zoals elke regel waarvan de bron ontbreekt, `v130`), `beleggenKlaar()` vindt die rij niet en komt op `volledig:false` met blokkade `buffer`, de verse-start-kaart op Home laat zijn zin weg (die guardde al op `M.spaar!=null`), en `spaarVrij()`/`spaarOver()` zwijgen (`v126`). **`planMap()` kreeg wel een aanpassing**: die zette `gespaard` op 0, en een stellige nul is net zo onwaar als een geraden bedrag. De rekenkant houdt 0 aan zodat de verdeling niet verandert, maar het item draagt `spaarOnbekend` en de rij toont 'onbekend / €doel' met de reden en een tik naar `openSpaarrekening()` — dezelfde vorm als de onbekend-hero op Home, geen tweede vorm. `planSub()` laat de looptijd dan weg: een eta op een geraden nul is precies de stellige uitspraak die hier niet hoort. **Twee bekende grenzen, bewust niet opgelost:** een spaarrekening zonder boekingen bestaat niet in `OWN` (`v122`), dus die telt hier als onbekend ook als je er een saldo bij hebt gezet; en `fireInputs()` geeft `nfCur:0` in plaats van onbekend, want laag B is puur en rekent met getallen (`v32`), waardoor de noodfonds-mijlpaal op het vermogensscherm 0% toont. Dat zijn beslissingen, geen opruiming.
- **Eén definitie voor "wat blijft er over deze maand"** (`v169`): het variabele deel werd op vier plekken anders berekend. `safeToSpend()` had de `potjeRest`-lus uit `varPlanRemaining()` woordelijk overgeschreven, `nogDezeMaandBody()` rekende met `L.varDue` (een extrapolatie van je tempo) en `coachStatus()` had een derde formule op het historisch gemiddelde. Gemeten op dezelfde data, halverwege de maand: €150 plan tegen €931 prognose, dus Home zei '+€1.450 veilig te besteden' terwijl Inzichten '-€931' toonde. **`varPlanRemaining()` is de enige bron**, en elke plek roept hem aan in plaats van te herrekenen. Potjes zijn je sturingsmechanisme, dus het plan stuurt; het tempo is een voorspelling en mag getoond worden, maar niet als grondslag. `potOver` in `safeToSpend()` houdt zijn eigen lus, want dat is een andere vraag (hoeveel ging je erover) en geen tweede antwoord op deze. **De twee schermen beantwoorden bewust een andere vraag en moeten toch aansluiten**: Home telt je saldo mee en reserveert je spaardoel, Inzichten kijkt alleen naar de stromen van deze maand. Een test eist in vijf situaties dat `safe === netto + spendSaldo - saveReserved` exact klopt; loopt dat uiteen, dan is er een tweede definitie teruggeslopen. `L.varDue` blijft bestaan en voedt nog precies één ding: `varForecast`, en dat is de spiegel in de opbouw-sheet die je prognose náást je plan zet. **Bekend en niet aangeraakt:** `L.varPlan` wordt door niemand gelezen sinds de liquiditeitskaart verviel, maar `monthLiquidity()` valt buiten deze ronde.
- **Nog te betalen mengt geen waarneming met een prognose** (`v169`): de tegel telde `fixDue` (herkende incasso's, waargenomen) op bij het variabele deel (voorspeld) tot één rood bedrag. Twee soorten zekerheid in één getal is niet te lezen en niet te controleren. De tegel heet nu 'Nog te betalen · vast' en toont alleen de waarneming; het variabele deel staat als eigen regel eronder ('plus €X variabel uit je potjes'), met een tik naar `openReservedPotjes()`. 'Deze maand op eigen kracht' trekt beide af van wat er nog binnenkomt, maar de twee blijven apart zichtbaar. Een bedrag van nul kleurt niet rood maar `--mut`, zoals de tegel ernaast al deed.
- **De naam zegt wat het getal is** (`v170`): 'Onderaan de streep' heette een stroomsaldo maar las als besteedbare ruimte, en stond rood naast een positief 'Veilig te besteden' op Home. Het heet nu **'Deze maand op eigen kracht'** (`jrg('eigenkracht')`): wat er deze maand nog binnenkomt min wat er nog uit moet, **zonder je banksaldo**. Negatief betekent dat er meer uitgaat dan er binnenkomt en je dus je saldo aanspreekt; de uitleg verwijst voor het bedrag mét saldo naar Home. Bewust **niet** het saldo meegenomen: dan staat hetzelfde cijfer op twee schermen, precies wat `v169` weghaalde. De twee sluiten aan via `safe === eigenKracht + spendSaldo - saveReserved`, en die identiteit staat als test vast in vijf situaties. Het woord 'saldo' blijft gereserveerd voor je banksaldo (`v91`).
- **`L.varPlan` is weg** (`v170`): hij voedde de spiegel op de liquiditeitskaart, die verviel, en sindsdien las niemand hem. Wie het variabele plan nodig heeft roept `varPlanRemaining()` aan. Dat is de enige uitzondering op 'monthLiquidity() blijft ongemoeid': één veld zonder lezer, geen logica.
- **Home toont één bedrag per vraag** (`v171`): daar stond hetzelfde getal drie keer. De hero-subregel 'totaal saldo' en de Expert-voetregel kwamen **letterlijk uit dezelfde bron** (`totalBalance().sum`), en `vermogenCard()` zette er 'Netto vermogen' onder: een ánder getal (`netWorth()` = saldo + bezittingen + uitgeleend − schulden − geleend) dat er alleen gelijk aan lijkt zolang die drie leeg zijn. Zodra je één post invult lopen ze uiteen zonder dat het label zegt welk je leest. **Totaal saldo hoort op Home**, want Home beantwoordt 'wat kan ik nu doen' en dat leunt op je rekeningen; het vermogen staat één keer, op zijn eigen scherm. `vermogenCard()` is nog steeds de ingang maar draagt **geen bedrag**: hij zegt hoeveel bezittingen en schulden je hebt ingevuld, en heet daar ook naar. Zet er nooit een bedrag terug: een kaart die een getal toont en in dezelfde adem om de invoer vraagt die het compleet maakt, toont per definitie iets onvolledigs. Let op dat `netWorth()` ook **uitgeleend en geleend geld** meetelt (`v77`), dus het verschil kan ontstaan zonder dat er één bezitting is ingevuld.
- **Hele euro's waar het een samenvatting is, centen waar het waargenomen is** (`v171`): op Home tonen het hoofdgetal en de saldo-regel hele euro's; de transactielijst eronder en de saldo-drill-down per rekening houden hun centen, want daar is het bedrag exact en waargenomen. Twee bedragen vlak onder elkaar in verschillende notatie lezen als twee verschillende soorten getal. Dit is voorlopig alleen op Home doorgevoerd.
- **In het plan is elke voortgang een toewijzing, ook die van het noodfonds** (`v172`, model B; trekt `v102` in): de kolom 'gespaard / doel' had twee herkomsten. Bij het noodfonds kwam het linkergetal uit `spaarSaldo()` (een gemeten rekeningsaldo, herrekend als `min(saved, doel)`), bij een spaardoel uit het handmatige veld op het goal-object. Identieke weergave, en samen konden ze meer claimen dan er stond zonder dat de gebruiker iets fout deed. **Model A was niet consistent te maken**: een spaardoel heeft geen rekening en geen transacties (`v99`), dus er valt niets aan te meten - toegewezen is de enige kant waarop consistentie bereikbaar is. `planMap()` leest nu `SET.nfToegewezen`, geklemd op het doel, en roept `spaarSaldo()` niet meer aan. **Het label draagt de betekenis**: de rij toont '€X toegewezen / €Y', nooit impliciet. `spaarVrij()` is daarmee één aftrekking over alle items zonder uitzondering, en `spaarVrijToe()`/`spaarVrijDoel()` mogen ook het noodfonds aanvullen (een aflos-item niet, dat leidt zijn voortgang af uit de restschuld, `v100`).
- **De gemeten buffer blijft gemeten, en zegt dat** (`v172`): `bufferMaanden()`, de bufferregel op Maand en `beleggenKlaar()` lezen onverkort `noodfondsModel().spaar`, dus `spaarSaldo()`. Ze mogen **nooit** op een toegewezen bedrag gaan leunen: of je genoeg buffer hebt om te beleggen is een feitelijke vraag over je rekening, en een handmatig cijfer dat niet meebeweegt mag die niet beantwoorden. De eenheid op de bufferregel zegt daarom expliciet 'maanden op je rekening'. Het plan-item en die regel verschillen dus legitiem: twee vragen, twee labels.
- **`spaarOver()` is met zijn oorzaak vervallen** (`v172`): die functie bestond uitsluitend om het verschil te melden dat uit de menging ontstond. `spaarStil()`, `spaarOverDoelen()`, `spaarOverLine()` en `spaarOverAf()` gingen mee. Sinds elke voortgang een toewijzing is kan de som het saldo alleen overschrijden als je zelf te veel toewijst, en dat houdt `spaarVrijToe()` al tegen. De aansluitingsregel op Maand houdt alleen zijn vrij-helft.
- **De vrij-regel is het enige bijstuurmechanisme, dus hij mag niet in de ruis verdwijnen** (`v172`): `SET.nfToegewezen` komt eenmalig uit `migrateNfToegewezen()` (`min(spaarSaldo().cur, doel)`, alleen bij een bekend saldo, vlag `nfToegewezenMigrated`) en loopt daarna stil, net als de restschuld bij `v100`. Wat het bijstuurt is `spaarVrijLine()`: die staat als eigen blok met de linkerrand die de verse-start-kaart al gebruikt, niet als gemuteerde regel tussen de andere. Geen amber, want er is niets mis - er valt alleen iets toe te wijzen (`v78`/`v93`). Bij een onbekend spaarsaldo toont datzelfde blok de reden en de tik naar `openSpaarrekening()`; die stond eerder in de plan-rij (`v168`) en die rij toont nu een toewijzing.
- **Zwijgen is de uitkomst, niet een nettere schatting** (`v173`): zeven plekken toonden een stellig getal of oordeel op data die er niet is. Er is **één model** en dat stond al in de app: de onbekend-hero op Home. Geen stellige nul, wel de reden, wel één volgende stap. Ontwerp daar geen tweede vorm naast en vervang geen enkel geval door een gemiddelde of een terugval; kan een plek daardoor niets tonen, dan is dat het juiste resultaat. Wat er per plek veranderde: **(1)** de noodfonds-rij op Vooruitblik toonde `€0 toegewezen / €4.880` met een tempo en een einddatum, terwijl `migrateNfToegewezen()` nooit kon draaien omdat het spaarsaldo onbekend was. Die nul was geen toewijzing van jou maar een gat, dus `planMap()` zet `nfOnbekend` en de rij laat bedrag, balk, tempo en datum alle vier weg. **(2)** `totals().budget` valt zonder potjes terug op de inkomen-limiet; de hero noemde dat 'maandbudget'. Hij heet nu 'je inkomen-limiet', precies zoals de kerncijfers hem al noemden - alleen de naam, het getal blijft. **(3)** een boeking van vóór de getoonde maand gaf '€0 uitgegeven, 0%' als feit. `laatsteImport()` en `importCta()` zijn de **enige** detectie en de **enige** tik, gedeeld door `renderReminder()` en `insBudgetBlok()`. **(7)** de mededeling over te weinig historie stond per tegel én onder de maandgrafiek: drie keer op één scherm. De grafiek zegt het nu als enige.
- **Een aandeel alleen kan onvolledigheid niet meten** (`v173`): 'Er ontbreekt te veel om een oordeel te geven' vuurde bij één onbekende regel van één, want 100% ligt boven elke drempel. Er hoort dus een ondergrens bij: `MAAND_DREMPEL.onbekendDeel` (0,5) **en** `onbekendMin` (3). Onder de drie regels zegt een verhouding niets over volledigheid; dan valt de lijst terug op 'Er is nog niets te beoordelen' met de bestaande opsomming van wat onbekend is - dezelfde zin als bij een lege lijst (`v167`), geen nieuwe formulering. Let op de tussenstand die daar ontstond: zonder die terugval telde de telzin uit `v167` er nul en stond er 'De 0 regels die te toetsen zijn staan goed'; een test loopt alle combinaties tot vijf regels af.
- **Een spiegel zonder gevolg, of een gevolg zonder keuze, is onvolledig** (`v174`): vier plekken toonden een stand zonder wat het betekent, of een gevolg zonder uitweg. **'Valt op'** toonde alleen het wat; de duiding (`hyp`) en het dus-wat (`imp`) lagen sinds `v166` als comment bewaard en staan nu **woordelijk** terug in `insSignals()`. Herschrijf ze niet: ze zijn geschreven toen de signalen gemaakt werden. **Een gevolg wordt nooit verzonnen om de vorm compleet te maken**: het piekdag-signaal krijgt geen tik, want er is geen weergave per weekdag en een tik naar iets anders zou een stap suggereren die er niet is. Dat signaal draagt `spiegel:true` en de regel zegt 'Alleen een observatie: hier hoort geen stap bij.' Het winkel-signaal kreeg wel een tik, want de winkelnaam stond al in `kpiSub` en `openMerchant()` bestond al. **Een tekort** kreeg zijn volgende stap alleen in Rustig, precies omgekeerd aan wat de modusnamen beloven; dezelfde zin en dezelfde route staan er nu in elke modus, en alleen de aanhef en de gedempte kleur blijven van Rustig. **De verse-start-kaart** opende met 'je speelruimte staat weer vol' boven een negatieve Veilig te besteden; hij leest nu `safeToSpend()` - dezelfde bron als de hero eronder - en constateert in beide richtingen even neutraal, met een derde variant bij een onbekend saldo. **`coHorizonVraag()`** noemde bij meerdere doelen met een tekort er één; `coHorizonMeer()` telt de rest en de zin noemt dat, zonder een tweede doel aan te bieden. Niets in deze laag mag aanmoedigen, belonen of scoren: een test toetst dat op 'goed bezig', 'streak', uitroeptekens en em-dashes.
- **`pctWord()` kwam terug als `pctWoord`** (`v174`): de piekdag-zin gebruikte hem en hij ging in `v165` mee als dode code. De vervanger noemt het percentage zelf ('Ongeveer 91%') zodat de rest van de zin woordelijk blijft; hij vertaalt niet opnieuw naar 'bijna de helft'.
- **De twee kaarten op Maand putten uit dezelfde lijst** (`v175`): `beslis` filterde op `RO` en `aandacht` op `R`, dus een structureel signaal met status `let op` landde in geen van beide kaarten en telde alleen mee in de oordeelzin. Dat is **verlies, geen weergavefout**: het scherm noemde het in de kop en toonde het nergens. Beide filteren nu op `RO`. `goed` blijft op `R`, want een structureel signaal is per definitie geen `ok`.
- **De zwaarte van een structureel signaal komt uit het signaal zelf** (`v175`, verfijnt `v162`): `MAAND_DREMPEL.structureelStatus` duwde alles naar `tekort`, ook de `info`-variant, waardoor een neutrale observatie de oordeelzin naar 'er is iets dat vastloopt' trok. `STRUCT_STATUS` leest `n.t`: `bad` en `warn` zijn een **blokkade** en landen in 'vraagt een beslissing', `info` is een **observatie** en landt in 'vraagt aandacht'. **Twee bestemmingen, nooit een derde**: past een signaal in geen van beide, maak er dan geen lichtere status voor maar stel de vraag of het signaal nog nodig is - een derde categorie valt nergens meer op. `structureelStatus` blijft de terugval voor een onbekende `t`.
- **De coach-ingang en de oordeelzin wijzen altijd naar dezelfde regel** (`v175`, trekt de uitzondering uit `v162` in): `maandCoachIngang()` en `maandVerband()` kregen `R` zonder de structurele regels, dus de ingang onderaan wees naar het lichtere item terwijl de kop het zwaardere noemde. Beide lezen nu `RO`. De reden om dat níet te doen is weggenomen in plaats van omzeild: `coMaandRegel()` zoekt na `maandRegels()` ook in `maandStructureel()`, en `coTopicMaand()` valt alleen terug op de zwaarste regel als de sleutel **nergens** op slaat - en dan op dezelfde lijst als de ingang zelf. Een structureel signaal krijgt in het gesprek zijn eigen actie aangeboden, precies de tik die de rij op het maandscherm ook heeft; geen `coachRuleOptions()` daar, want die gaan over een uitgavecategorie en dit gaat over een patroon over maanden. Een test bindt de ingang aan `coMaandZwaarste(RO)` in drie situaties (alleen observaties, alleen blokkades, beide), zodat ze niet opnieuw uiteen kunnen lopen.
- **Eén maandkiezer, en die zit in `curMonth`** (`v176`, trekt de eerste helft van `v166` in): Inzichten en Maand konden alleen de lopende maand tonen. De kiezer schrijft nu `curMonth`, en dat is **bewust geen veld in `SET`**: `curMonth` is module-state, dus de keuze verdwijnt bij een herstart en bij een import (die zet `curMonth=null`), en elk drill-down leest hem al. Een keuze in `SET` zou een instelling worden die over sessies blijft hangen, en dan zit je over drie weken nog in juli zonder het te merken. **Duur van de keuze: de sessie**, geen timer. Bereik: uitsluitend `months()`, dus geen toekomst en niets vóór je eerste boeking; `zetKijkMaand()` weigert een maand die daar niet in staat. `kijkMaand()` en `isLopendeMaand()` zijn de enige twee lezers die je nodig hebt.
- **Bij een afgesloten maand verdwijnt alles wat over nu gaat** (`v176`): drie categorieën. **(a) rekent door** op de gekozen maand: de budgetstand (`insBudgetBlok`), 'Valt op' (`insSignals` is per maand), de kerncijfers (`insKpiStrip`, `maandKpiBlok`) en de meermaands-grafiek. **(b) gaat over het nu en verdwijnt**: 'Nog deze maand', 'Nog te betalen', de 'loopt nog'-noot, `subsCard()`, de lek-vraag en de coach-ingang. **(c) leest een saldo van vandaag en wordt weggelaten, niet gelabeld**: buffer in maanden, dekking reserveringen, aansluiting spaargeld, aankoopdoel en `beleggenKlaar()`. Onder een historische kop leest een getal als historisch, hoe je het ook bijschrijft. **Let op de val:** `patroon` hoort naar de letter bij (a), maar `maandPatroon()` leest `scoreNotifs()` en dat rekent intern op `ms[ms.length-1]` - altijd de lopende maand. Het valt dus feitelijk onder (c) en gaat mee weg; `scoreNotifs()` een maand meegeven is een wijziging aan de signalen-engine en hoort in een eigen ronde.
- **Het oordeel op Maand claimt niets wat er niet staat** (`v176`): omdat categorie c het hele regelblok is, valt op een afgesloten maand ook `maandOordeel()` weg - die telt de regels die er staan, en er staan er geen. In plaats daarvan één zin die zegt dat de maand-regels een stand van vandaag lezen en daarom niet met terugwerkende kracht bestaan, plus de kerncijfers die wél per maand rekenen. Vul dat gat niet met een oordeel over minder regels: dan beoordeel je iets anders dan het scherm toont.
- **Alleen kijken, en zichtbaar dat je kijkt** (`v176`): een afgesloten maand kent geen afspraak, geen coachgesprek en geen bewerking. `SET.maandGelezen` wordt er niet door gezet, dus het onderdrukt ook niets van het heden. De banner gebruikt bewust dezelfde `signal warn`-vorm als de herinnering over verouderde data: dat is de bestaande manier om te zeggen 'wat je ziet is niet het heden', dus geen nieuwe kleur. **De maandnaam in de kop van `insBudgetBlok()` is de kiezer geworden**; `openBudgetCompare()` hangt daardoor aan de dagteller ernaast. De ingang uit `v114`/`v115` blijft dus bestaan, alleen op een ander element - de ring die hem ook droeg is in `v166` al vervallen. Op Maand houdt de kaart zijn eigen titel 'Je maand' en staat de kiezer ernaast als chip.
- **Een tempo extrapoleert alleen het variabele deel** (`v177`, oorzaak 6: de kalender levert het oordeel): een tempo veronderstelt dat een uitgave gelijkmatig over de maand valt, en een incasso doet dat per definitie niet. Huur op dag 2 liet het tempo op dag 5 exploderen, en dat gebeurt elke maand bij iedereen in de eerste week. **`tempoProjectie(m)` is de enige bron**: vier plekken rekenden hun eigen tempo, drie daarvan met vaste lasten erin. Vaste lasten tellen als **waarneming** mee - wat er al is afgeschreven plus `monthLiquidity().fixDue` - en worden nooit gedeeld door de verstreken dagen. De scheiding komt uit `isFixed()` (`v55`), dezelfde partitie als `splitFixedVar()`. **Tweede val, even belangrijk**: een boeking die verderop in deze maand valt is al ingepland en hoort niet in een tempo dat over verstreken dagen rekent; `variLater` telt wel mee in de projectie maar niet in het tempo. Een afgesloten maand wordt niet geëxtrapoleerd, daar is `variTempo === variTot`. `varBudget()` geeft de variabele helft van je potjes via dezelfde `recurringCats()`-partitie die `varPlanRemaining()` gebruikt, zodat een variabel tempo tegen een variabel budget staat en niet tegen je hele budget.
- **De dag van vandaag is een lokale dag** (`v177`): `new Date().toISOString().slice(0,10)` geeft de **UTC**-dag, en tussen middernacht en 02:00 zomertijd is dat gisteren. 'Gelezen op' toonde daardoor de dag ervoor, en een boeking van vandaag matchte niet in `scoreNotifs()`. Datums in `TX` zijn lokale kalenderdagen, dus `vandaagYMD()` is dat ook; `thisYM()` en `nowYMstr()` deden het op maandniveau al goed. De PSD2-aanroepen houden bewust hun ISO-datum: dat is een API-parameter, geen dag van jou.
- **Vier schermen, vier horizonnen, en een element staat op precies één** (`v178`): Home vraagt waar sta ik nu, Inzichten hoe loopt deze maand (operationeel), Maand houdt mijn systeem stand (structureel), Plan waar gaat mijn spaarinleg heen. Vijf elementen stonden op het verkeerde scherm en zijn verhuisd: **'vanaf volgende maand'** en **'boven je inkomen-limiet'** (nu samen in `maandPlanRegels()` op Maand - het eerste gaat over een periode die nog niet begon, het tweede weegt je potjes tegen je inkomen-limiet en is een spiegel, geen plafond (`v53`)), de **meermaands-grafiek** `spendVsBudgetChart()` (die zet deze maand naast eerdere maanden, en dat is een structurele vraag) en **`subsCard()`** (een lastenlijst, niet iets over deze maand). Verplaatsen is nooit kopiëren: een test leest beide schermen en eist dat het element op het ene staat en op het andere niet. **`toggleCollap()` doet daarom `render()` in plaats van `renderIns()`** - hij ging ervan uit dat elke inklap op Inzichten stond, en de grafiek houdt zijn drietraps default uit `v90` ongewijzigd mee (`v137`, dezelfde reden). Op Maand vallen die elementen bij een afgesloten maand vanzelf weg: `renderMaand()` keert daar al eerder terug (`v176`), en dat is juist - je plan is een instelling van vandaag en abonnementen lopen nu.
- **De ingeklapte kop noemt wat eronder staat** (`v178`): `insKpiSamenvatting()` las `['inleg','budget']` terwijl diezelfde kaart uitgeklapt `budget` en `vari` toont - de spaarquote verhuisde in `v161` naar het maandscherm en de kop bleef hem noemen. Een samenvatting die een cijfer noemt dat er niet staat is erger dan geen samenvatting.
- **Een maandregel laat je niet van scherm wisselen** (`v178`): de rij `aansluiting` deed `go('vooruit')` en de terugval van `patroon` deed `go('ins')`, midden in je maandmoment. De eerste opent nu `openAansluiting()`, een sheet die **uitsluitend `spaarVrij()` leest** en met `spaarVrijDoel()`/`spaarVrijToe()` exact dezelfde toewijzing aanbiedt als `spaarVrijLine()` op je plan - er is geen tweede route naar een toewijzing (`v172`) en er wordt niets herrekend. De tweede opent `openMonthSpend(kijkMaand())`, ook een sheet. **Bewust ongemoeid:** draagt het patroonsignaal zijn eigen `act`, dan blijft die staan, ook als die naar een ander scherm gaat - dat is de consequentie van dat signaal en die is overal dezelfde; hem overschrijven zou het signaal een ander gevolg geven dan in de meldingenlijst.
- **Het scherm heet Plan, de sleutel blijft `vooruit`** (`v179`): het heette Vooruitblik maar bevat de plan-editor. De vraag die je er beantwoordt is niet 'hoe ziet mijn toekomst eruit' maar **'waar gaat mijn spaarinleg als eerste heen'**, en dat is een beslissing - de naam dekt nu de beslissing in plaats van de projectie. Hernoemd zijn de nav en **elke tekst die je leest**: het spaardoel-signaal ('Bekijk je plan'), de twee coach-slotzinnen ('Dat zie je terug in je plan'), de noodfonds-verwijzing in Instellingen en de modus-uitleg. **De interne sleutel verandert niet mee**: `s-vooruit`, `go('vooruit')`, `SET.vooruitDoelOpen` en de waarde `vooruit` in `minder_view` zijn opslag en navigatie-state, geen tekst - hernoemen zou een migratie vragen zonder dat er iets aan verandert, en een half doorgevoerde hernoeming van een opslagsleutel kost je je laatst bekeken scherm. De voetregel 'Vooruitkijken is een schatting, geen belofte' blijft: die zegt wat de getallen zijn, niet hoe het scherm heet.
- **Een instelling belooft alleen wat bestaat** (`v180`, de schrappenlijst uit de Instellingen-audit): zeven items, allemaal iets dat werd geschreven of getoond zonder dat er iets achter zat. **De schakelaar 'Streak tonen'** schreef `SET.hideStreak` en niemand las het: de streak verdween in `v159` en `v130` legde al vast dat er geen streak en geen teller is. **De schakelaar 'Demo: mechanismen tonen'** nam `demoNotifs()` mee (twee verzonnen meldingen), de verzonnen geparkeerde aankoop in `coParkReturn()` met zijn drie `p.__demo`-guards, de `-demo$`-uitzondering in de loss-frame-dosering en de tak in `freshStartDue()` die het verse-start-venster altijd openzette. De twee `v.__demo`-guards op `SET.maandVoornemen` gingen mee: **niets schreef die vlag ooit**, dus ze waren altijd waar. **`SET_SHEETS` houdt nog twee mappings**: `openSet()` heeft in de hele app twee aanroepers, `openSaldoInvoer()` met `income` en `openSpaarrekening()` met `bank`. De zes andere (look, budget, fire, modus, coach, privacy) waren onbereikbaar; hun panelen bestaan onveranderd en klappen inline uit met `toggleSet()`, behalve budget dat via `openBudgetEditor()` zijn eigen sheet heeft. De hook `if(_setSheet==='look') renderSetSheet()` in `setTheme()` ging mee, want `_setSheet` kon nooit `'look'` worden; het paneel ververst nu live via `render()` -> `renderSet()`, dezelfde uitkomst. **Het `fn`-argument bij Budget & doelen is `null`**, zoals de coach-avatar-regel al deed: `fn` en `sheet` sluiten elkaar uit in `renderSet()`, dus `setBudget` werd daar nooit aangeroepen. **'Coach & categorieën' heet 'Coach'**: er bestaat nergens een categoriebeheer. **Het thema-voorbeeld toont je eigen bedrag** via `themeHeroTekst()` - `euro0(safeToSpend().safe)`, en bij `totalBalance().known===0` hetzelfde woord dat Home daar toont (`v59`/`v73`). Er staat nergens anders in de app een verzonnen cijfer, dus ook niet in een voorbeeld dat over kleur gaat. **Niet geschrapt: de regel over een rekening zonder boekingen** - die heeft wél een gevolg (hij opent `openRekOverlap()`, dat vertelt waarom die rekening nergens anders staat) en er ontbreekt juist een actie: samenvoegen. Dat is precies het `v122`/`v150`-geval dat `rekeningOverlap()` structureel niet kan vinden, want er zijn geen boekingen om te vergelijken. De regel weghalen zou de enige ingang naar die actie weghalen.
- **Een instelling die je dagelijks gebruikt zit niet achter een easter egg** (`v181`, oorzaak 1 uit de Instellingen-audit): 'Bestand toevoegen', 'Map koppelen' en 'Synchroniseer map' zaten achter `bankAdvTap()`, vijf tikken op de voetregel van Instellingen. Voor wie geen backend heeft is dat de enige manier om data in de app te krijgen. Ze staan nu zichtbaar onder Bankkoppeling, **bewust als tweede optie**: de PSD2-koppeling blijft de hoofdroute, houdt de plek bovenaan en houdt als enige het accent (`btn`); alles in het bestandsblok is `btn sec`. Keer die verhouding niet om. **Backend-URL en app-token blijven wel achter Geavanceerd**, want dat zijn ontwikkelaarsinstellingen. Daarom mag `psd2Connect()` niet stil falen: hij deed een toast ('Stel eerst je bank-verbinding in') plus `SET.advBank=true`, dus hij veranderde ongevraagd een instelling en zei niet wat een bank-verbinding is of waar hij vandaan komt. Zonder backend opent hij nu `psd2GeenBackend()`: wat er nodig is, dat Minder nooit rechtstreeks met je bank praat, en wat je zonder koppeling wel kunt (een bestand toevoegen). De velden tonen blijft een eigen tik (`psd2ToonGeavanceerd()`). **De AI-coach: de schakelaar blijft onder Coach, de bouwinstructie gaat naar Geavanceerd** - het `/coach`-endpoint, de LLM-key en het modelveld staan nu naast de backend-URL die ze nodig hebben. De subregel zegt wat er verandert (een taalmodel verwoordt de zinnen, je cijfers blijven uit de lokale motor, de coach-tekst gaat naar je eigen backend), niet hoe je het bouwt; staat hij aan zonder backend, dan zegt de app dat de lokale coach aan het woord blijft.
- **Een claim op dit scherm volgt de werkelijke stand** (`v181`, oorzaak 2): drie beweringen konden onwaar zijn. **(1) De privacyregel** zei onvoorwaardelijk 'Alles blijft op dit toestel', en dat wordt onwaar zodra de bankkoppeling of de AI-coach aanstaat. `privacyUitzonderingen()` is de enige bron voor zowel `privacySub()` als de paneeltekst, dus de subregel en de uitleg kunnen niet uiteen lopen; de paneeltekst noemde alleen de bankkoppeling en noemt nu allebei. Gemeten aan wat er **daadwerkelijk** gebeurt: een gekoppelde bank (rekeningen in `psd2Accounts`) haalt bij elke verversing data op, en een aangezette AI-coach stuurt coach-tekst weg. Een ingevulde backend-URL zonder koppeling telt dus niet: er gaat dan niets weg. Een privacyclaim die niet klopt is de duurste tekst op dit scherm. **(2) De bankstatus** toetste alleen het aantal rekeningen en meldde 'Je bank is gekoppeld' terwijl het paneel eronder 'Verbinding verlopen' toonde. `bankStand()` is nu de enige bron voor de subregel en het paneel. **(3) De kleur is omgedraaid**: amber stond op 'Nog geen bank gekoppeld' (daar is niets aan de hand, je hebt alleen nog niets gedaan) en de verlopen verbinding bleef grijs. Amber is voor echte aandacht (`v78`/`v93`), dus hij hoort bij verlopen en nergens anders.
- **Ook een subregel in Instellingen zwijgt bij onbekend** (`v181`): 'Je spaart €0 per maand' en 'Je ontvangt €0 per maand' stonden er als feit in een lege app, en 'Je rekeningen & saldo · €0,00 totaal' zonder één bekend saldo. Dat zijn precies de plekken waar Home 'onbekend' zegt. `inkomenBekend()` en `spaardoelBekend()` zijn de toets; bij onbekend staat er het woord **onbekend** en niet een nul, en de tik naar de invoer zit al in de regel zelf. Geen tweede vorm en geen tweede formulering: 'saldo onbekend' bestond al in de rekeningenlijst van `setBank()`. `spaardoelBekend()` kijkt bij `savingMode:'amount'` naar het ingevulde bedrag en anders naar het inkomen, want daar is de uitkomst uit afgeleid.
- **Een instellingsregel staat bij zijn onderwerp, niet bij zijn code** (`v182`, oorzaak 3 uit de Instellingen-audit): drie van de zes items zijn gebouwd, drie wachten op een keuze. **De AI-coachschakelaar staat onder Bankkoppeling**, niet onder Coach: hij draait op dezelfde backend-URL als de bankkoppeling en zijn instructie staat daar sinds `v181` ook. Hij staat er **zichtbaar**, niet in het geavanceerde blok - dat is een gebruikersknop, en die verstoppen is precies wat `v181` ophief; alleen het modelveld en de bouwinstructie blijven onder Geavanceerd. Daarmee is Coach wat hij hoort te zijn: de lokale coach, zonder backend erin. **De twee coach-regels zijn er één.** 'Coach & categorieën' klapte inline uit en 'Coach-avatar & toon' opende een sheet: één onderwerp, twee regels, twee gedragingen. Er is nu één regel `coach` die inline uitklapt, met daarin een rij naar `openCoachAvatar()`. **De sheet zelf is onveranderd en houdt al zijn ingangen**: de coachkop op `s-act`, `setCoachTone()` die zichzelf heropent, en de rij in het paneel. Bouw daar geen tweede editor naast (`v61`): het paneel verwijst, het stelt niets zelf in. De subregel van de regel noemt allebei: 'Je coach staat aan · Sara · directe toon'. **De volgorde loopt van data naar vorm**: Inkomen & rekeningen, Bankkoppeling, Budget & doelen, Vermogensreis, Coach, Weergave & modus, Uiterlijk, Privacy & gegevens. Eerst wat er binnenkomt, dan wat je ermee van plan bent, dan hoe het verteld wordt, dan je gegevens zelf met het wissen onderaan. Uiterlijk en Weergave stonden boven Inkomen en Bankkoppeling: cosmetica boven data.
- **De laatste vier van de sectie-indeling** (`v183`, oorzaak 3 afgerond): **De sectie heet 'Bank & koppelingen'**, want hij draagt drie manieren waarop Minder met de buitenwereld praat: de PSD2-koppeling, de bestandsimport en de AI-coach, plus sinds nu de briefingexport. **'Je naam' staat onder Coach**: de enige lezers van `SET.name` zijn `coVoornaam()` en de begroeting in het gesprek, en het veld zei het zelf al ('je coach spreekt je hiermee aan'). **'Transacties & categorieën' is een nieuwe sectie** met vooralsnog één schakelaar, 'Interne overboekingen verbergen'. Dat is een categorisatieregel: hij bepaalt of een boeking als uitgave telt. Onder Inkomen zetten zou hem verbergen, en dan is de plek er niet zodra de regel-editor wel gebouwd wordt. **Nog open, en een echte bevinding:** `SET.rules` is alleen vanuit het transactiedetail te maken en staat nergens in Instellingen; deze sectie is de plek waar die editor hoort te komen. **De briefingexport staat bij de koppelingen**, want Privacy gaat over waar je data staat en dit is een uitgaande koppeling met een andere app. Hij komt bewust **niet** in `privacyUitzonderingen()`: die functie beantwoordt wat er van je **toestel** af gaat, en de export schrijft naar een map op ditzelfde apparaat. Privacy noemt hem nog wel, als feit met de plek erbij, want dat Minder automatisch bij openen naar een map schrijft hoort daar te staan.
- **Eén lijst voor je rekeningen** (`v183`): `accountsCard()` (naam en spaarvlag, onder Bankkoppeling) en de lijst in `setIncome()` (naam en saldo-invoer) gingen allebei over dezelfde rekeningen, liepen allebei over `zichtbareRek()` en toonden allebei naam en saldo; alleen de bewerking verschilde. `accountsCard()` is nu de enige lijst, met per rekening het saldo **en** de vlag, en woont onder Inkomen & rekeningen. Bank & koppelingen houdt `rekOverlapRegel()`, want twee ids met dezelfde boekingen is een koppelingsprobleem, en verwijst voor de rest naar de lijst. `legeRekRegel()` rendert daarmee nog één keer in plaats van twee. Elke rij draagt `data-acc` met zijn rekening-id, zodat een verwijzing niet op opmaak hoeft te mikken. Een onbekend saldo houdt het woord dat de app er al voor had: 'saldo onbekend' als label bij het lege invoerveld, niet een stellige nul (`v59`/`v73`). De naam komt uit `acctNiceName()`, dus uit `acctNaam()` (`v118`) - `setIncome()` gebruikte daar nog `accShort()`, dat daarbuiten omgaat.
- **`openSpaarrekening()` wijst naar `income`, en `SET_SHEETS.bank` blijft staan zonder aanroeper** (`v183`): de spaarvlag staat sinds de samenvoeging in de lijst onder Inkomen & rekeningen, dus beide aanroepers van `openSet()` geven nu `income` mee. Daarmee heeft `SET_SHEETS.bank` geen aanroeper meer. **Dat is een vastgelegde uitzondering op `v180`, geen vergeten mapping**: haal hem niet weg in een opruimronde, want oorzaak 4 (`SET_SHEETS` als tweede oppervlak) gaat juist over de vraag of dat mechanisme blijft bestaan, en die ronde heeft de bank-sheet nodig om die vraag te kunnen beantwoorden. Een test legt vast dat beide aanroepers `income` zeggen en geen enkele nog `bank`.
- **Klaar keert terug naar waar je vandaan kwam** (`v184`): de knop deed hardgecodeerd `go('dash')`, dus wie via de budgetlink op Inzichten of via een rij op Maand in Instellingen belandde werd op Home gezet, zonder terug. De herkomst komt uit de **bestaande view-state**: `go()` schrijft `minder_view` pas aan het eind, dus aan het begin staat er nog het scherm waar je vandaan komt. Dat is **één waarde, geen tweede navigatiegeschiedenis**, en hij staat bewust in module-state (`setHerkomst`): na een herstart is er geen herkomst en valt `setKlaar()` terug op Home, precies het randgeval - dezelfde redenering als `curMonth` in `v176`, door constructie in plaats van door een opruimregel. `go('set')` vanuit Instellingen zelf overschrijft de herkomst niet (`v!=='set'`), zodat `openSpaarrekening()` terwijl je er al staat je niet in Instellingen opsluit; elk ander scherm wist hem. `setKlaar()` verbruikt de waarde, dus twee keer Klaar valt de tweede keer terug. Sla dit nooit op in `SET` of `localStorage`: dan zou je over een week nog terugkeren naar een scherm waar je toen vandaan kwam.
- **De driehoek vouwt, de chevron gaat ergens heen** (`v185`, oorzaak 4): een instellingsregel klapt inline uit of opent een sheet, en aan de regel zag je dat verschil niet - allebei droegen ze dezelfde chevron. **Beide patronen zijn met opzet gekozen** (inline zodat je je plek niet verliest, een sheet zodat er één oppervlak per editor is, `v61`/`v137`), dus het gedrag blijft en alleen het teken zegt welk van de twee je krijgt. Een regel met een sheet houdt `›`, een regel die uitklapt draagt `▼` dicht en `▲` open. Dat is wat de chevron overal elders in de app al betekent; zet er geen kleur of tweede markering bij.
- **`SET_SHEETS` is opgeheven** (`v185`, trekt de uitzondering uit `v183` in): de tabel begon met acht ingangen, hield er in `v180` twee over en in `v183` nog één aanroeper. Een tabel van twee met één aanroeper is geen tabel meer, en hij droeg een **tweede set labels**: de sheet heette 'Bankkoppeling & import' terwijl de regel 'Bank & koppelingen' heet. `openSet()` en `renderSetSheet()` zijn vervangen door `openInkomenSheet()`/`renderInkomenSheet()`, die die ene sectie rechtstreeks openen met exact de naam van de regel. De bank-ingang is **niet bewaard voor het geval dat**: een mapping zonder aanroeper is geen voorraad maar ballast (`v165`), en komt er ooit een ingang bij, dan is dat vijf regels werk. `window._setSheet` blijft wel bestaan als markering van het oppervlak, want `openCoachAvatar()`, `openBudgetEditor()` en het coachgesprek zetten hem op null om de sheet over te nemen.
- **Een instellingenpaneel toont aannames, geen uitkomsten** (`v185`, herzien 20/21): het paneel Vermogensreis toonde 'Nodig: €448.500 · verwacht rond 2041' en 'Benodigd kapitaal €600.000 · in 2057 overschot €X'. Dat zijn doorgerekende uitkomsten in een scherm dat over invoer gaat. **De eerste is weggehaald** omdat hij letterlijk gedupliceerd was: de mijlpaal *FIRE-getal* draagt het bedrag en het scherm zegt al 'kom je rond 2041 bij je FIRE-getal'. **De tweede is verhuisd** naar `reisInkomensdoelRegel(M)` bij de mijlpalen, want die stond nergens in woorden - `reqCap` werd alleen als lijn in de grafiek getekend, en dan nog alleen met 'Inkomensdoel' aan. Meet dat verschil voordat je iets weghaalt: gedupliceerd mag weg, uniek moet mee. Er wordt niets herrekend, `reisModel()` is ongemoeid (`v32`).
- **Een waarschuwing hoort waar de projectie staat** (`v185`): `SET.reis.taxPct` draagt in Instellingen de disclaimer 'Ruwe plaatshouder, geen berekend feit', maar dat percentage voedt de hele vermogenslijn, en op het vermogensscherm stond het woord plaatshouder nergens. Wie het veld nooit opende, las de projectie als een berekening. De **bestaande** belastingzin daar ('Zonder belasting zou dat rond X liggen') noemt nu het tarief en zegt dat het een ruwe plaatshouder is waar deze hele lijn op rust. Geen nieuwe disclaimer en geen nieuw blok: de zin die er al over belasting ging, maakt zijn eigen grond expliciet. De disclaimer bij het veld blijft ongewijzigd - beide staan er, want ze bedienen twee verschillende lezers.
- **Eén terug-mechanisme voor alle drie de knoppen** (`v185`, verruimt `v184`): Vermogen en Transacties hadden dezelfde hardgecodeerde `go('dash')` als Klaar in Instellingen, dus het mechanisme is algemeen gemaakt in plaats van drie keer nagebouwd. `vorigeView` onthoudt bij elke sprong precies **één** stap, gelezen uit `minder_view` vóór `go()` hem overschrijft; `terug()` verbruikt hem en zet tijdens die sprong geen nieuwe (`_terugSprong`), anders stuurt een tweede tik je heen en weer in plaats van naar Home. **Sta je al op het doelscherm, dan houdt de herkomst zijn waarde**: zonder die uitzondering sluit `openSpaarrekening()` je op in Instellingen als je hem daarvandaan aanroept. Geen stapel, niets opgeslagen: na een herstart is er geen herkomst en valt elke knop terug op Home.
- **De horizon van een signaal bepaalt op welk scherm het staat, en dat is er precies één** (`v186`, herzien 36): de regel 'Patroon van de maand' las `maandPatroon()`, en die filtert `scoreNotifs()` op de sleutels `budget-`, `discr-` en `tempo`. **Alle drie dragen `h:'direct'`**, dus dit was letterlijk dezelfde melding die je ook in de meldingenlijst ziet - `NOTIF_HORIZON` laat `direct` en `correctie` door. De indeling uit `v162` zegt waar een signaal hoort: structureel op Maand, direct en correctie in de lijst. De regel is dus vervallen en Maand houdt er vier: dekking, buffer, aansluiting, doel. **`maandPatroon()` blijft bestaan, maar alleen als invoer voor `maandVerband()`**: dat verband zegt hoeveel van je gat die uitgave dekt, en dat zegt de melding zelf niet - een afgeleide is geen doublure. Toets bij twijfel de horizon van de onderliggende sleutel, niet de vorm van de regel; ze zagen er verschillend uit en waren hetzelfde. **Zichtbaar gevolg, bewust geaccepteerd:** de vier resterende regels hangen alle vier aan iets wat je zelf instelt (reserveringen, spaarrekening, doel). Wie transacties heeft maar niets heeft ingesteld, krijgt nu de terugval 'Er is nog te weinig ingesteld om je maand samen te vatten' waar `patroon` die vroeger in zijn eentje overeind hield. Dat is eerlijker dan een regel die een melding herhaalt, maar het maakt die terugval wel bereikbaar; hij fireert op `!R.length` en kijkt dus **niet** naar `maandStructureel()`.
- **Eén kaart voor wat opvalt, met het gesprek erin** (`v186`, herzien 40): 'Valt op' en de lek-vraag waren twee identiek vormgegeven eenregelige kaarten die tegelijk konden renderen, direct onder elkaar, over dezelfde soort bevinding. `insLekVraag()` is opgegaan in `whatStandsOutLine(m, nu)`: de bevinding bovenaan, het gesprek als voetregel binnen dezelfde kaart. Gaat de lek over dezelfde categorie als de bevinding, dan herhaalt de voetregel de naam niet; is er alleen een lek, dan draagt die de kaart; is er geen van beide, dan rendert er niets. De `nu`-vlag houdt de gespreksingang van een afgesloten maand af (`v139`: alleen kijken), en de duiding en het dus-wat uit `v174` staan onveranderd in dezelfde kaart.
- **Een streefdatum rekent terug, hij verandert de verdeling niet** (`v123`): `allocatePlan()` rekent van bedrag en tempo naar een datum (`eta = ceil(rest/alloc)`); `doelTempo(goal, alloc)` doet het omgekeerde en geeft `{maandenTot, benodigd, gat, haalbaar}` met `benodigd = ceil(rest/maandenTot)`. Puur, en `null` zodra er niets te zeggen valt: geen `streefdatum`, `rest <= 0`, of een datum die niet in de toekomst ligt. Naar boven afronden, nooit naar iets optimistischers. De streefdatum is **optioneel** en stuurt de waterfall niet: een doel zonder datum gedraagt zich exact als voorheen, en `SET.goals[i].streefdatum` blijft weg zolang je niets invult. `eta` en `doelTempo` lezen dezelfde `rest` en `alloc`, dus ze kunnen elkaar niet tegenspreken — "haalbaar" betekent per definitie dat `eta <= maandenTot`. `doelTempoLine()` is de enige plek waar er woorden omheen komen: spiegel (wat je nu inlegt), gevolg (wat de datum vraagt), verschil. Geen oordeel, geen aanmoediging, geen viering.
- **De spaargeld-controle hangt aan het plan, niet aan het noodfonds** (`v126`): `spaarVrijLine(P)` en `spaarOverLine(P)` staan op **planniveau**, naast `planWarn` en `vrijLine` boven de rijen — niet meer binnen de plan-rij met een `type==='noodfonds'`-guard. Anders verdween de controle geruisloos zodra het noodfonds gepauzeerd, bereikt of uit het plan was, dus juist wanneer je buffer op peil is en alles naar je doelen gaat. `spaarVrij()` bailt alleen nog op `S.missing`; zonder noodfonds-doel is `doel` gewoon 0 en is `vrij = max(saved − toegewezen, 0)`. In `spaarOver()` claimt het noodfonds bij `doel === 0` **niets** (`nf = 0`, niet `saved`) — met `saved` werd `over` gelijk aan `toegewezen` en dus altijd positief, wat verborgen bleef zolang de melding alleen op het noodfonds-item rendeerde. `vrij` en `over` kunnen per definitie niet allebei positief zijn; gebeurt dat toch, dan tonen beide regels niets en waarschuwt `spaarStil()` naar de console. `spaarVrijToe()`, `spaarOverAf()`, `spaarVrijDoel()` en `spaarOverDoelen()` blijven ongewijzigd: alleen de plek van de melding en de guards veranderen, niet hoe een verschil wordt opgelost.
- **Dekking en liquiditeit zijn twee vragen, twee paden** (`v128`): "krijgt mijn saldo een dip" is `liquidityDaily()`/`recurringSchedule()`; "is mijn reserveringenpot toereikend" is `dekking()`. Die laatste roept de eerste **niet** aan en detecteert niets uit transacties — `SET.reserveringen` is handmatige invoer. `verplichtingen(maanden)` rolt de lijst uit (een verstreken vervalmaand met interval rolt door, een verstreken eenmalige post vervalt); `dekking(maanden)` geeft `{benodigdPerMaand, benodigdeStand, werkelijkeStand, graad, gedektTot, gat, tekort, regels, volledig}`. `benodigdPerMaand` = Σ `bedrag / max(offset,1)` — een post die deze maand valt heb je nu in zijn geheel nodig. `benodigdeStand` = het **opgebouwde deel van het eerstvolgende voorkomen per post**: `bedrag × (interval − offset) / interval`, geklemd op `0..bedrag`; latere voorkomens binnen de horizon tellen niet mee en een eenmalige post draagt niets bij, want een kostenpost die je vandaag invoert geeft geen achterstand die er nooit was. `gedektTot` telt chronologisch af vanaf de huidige stand. Nooit doorrekenen op een onbekend saldo (`v59`/`v73`). De reserveringsinleg is **geen spaardoel**: `benodigdPerMaand` komt niet in `planItems()`, `allocatePlan()` of `planCapacity()`, anders concurreert de reservering met je spaardoelen en telt hij dubbel.
- **De reserveringsrekening staat buiten je spaargeld** (`v128`): `SET.resAcc` wijst precies één rekening aan en hangt uitdrukkelijk **niet** aan `isSavingsAcc()`, `SET.savingsAcc` of `n26SavingsAccounts()`. Geld op die pot is geen vermogen maar uitgestelde consumptie: het is al vergeven aan de tandarts en de gemeente. Telt hij mee als spaargeld, dan duwt elke storting de spaarquote omhoog en elke betaling eruit hem omlaag, en meten `totalSaved()`, `savedThisMonth()` en `savedNet()` iets anders dan ze horen te meten. Is de gekozen rekening tóch als spaarrekening aangemerkt, dan meldt de sheet de botsing met een tik om de spaarvlag uit te zetten — nooit automatisch corrigeren. **Het saldo van de pot valt buiten veilig-te-besteden** (`v129`): `safeToSpend()` trekt naast `totalSaved()` ook `resSaldo()` af, met drie klemmen — is de pot ook als spaarrekening aangemerkt dan zit hij al in `savedBal` en trekken we hem niet twee keer af; een onbekend saldo zit sowieso niet in `tb.sum`; en een negatief saldo klemt op nul, want je reserveert geen negatief bedrag terwijl de roodstand zelf gewoon in `tb.sum` blijft staan. De opbouw-sheet noemt de post apart. **Eén bekend gat, bewust niet gedicht:** een storting naar een eigen rekening is alleen `intern` als de app de tegenrekening herkent (`refNums`, of de N26-heuristieken); levert je bank die niet, dan telt de storting als uitgave.
- **Het maandscherm stelt samen, het rekent niet** (`v130`): `s-maand` leest vijf bestaande bronnen — `dekking()`, `bufferMaanden()`, `spaarVrij()`/`spaarOver()`, `allocatePlan()`+`doelTempo()` en `scoreNotifs()` — en sorteert hun uitkomsten. Elke bron in zijn eigen `try`: valt er een weg, dan valt alleen die regel weg, zonder foutmelding en zonder placeholder; het scherm werkt met drie van de vijf. Voeg hier **nooit** een eigen berekening toe: is er iets nodig dat nog niet bestaat, bouw het dan in de laag waar het thuishoort of meld het. De enige uitzondering is `bufferMaanden()` (`spaarSaldo().cur / noodfondsModel().essCrisis`, `null` bij een van beide onbekend), bewust toegevoegd omdat geen enkele functie de buffer in maanden uitdrukte. Drempels staan in één constante `MAAND_DREMPEL` (`bufferKritiek:3`, `dekkingOk:100`, `doelOk:0`), de rest volgt `nfMaanden()`. Let bij dekking op dat `graad` **null kan zijn zonder dat er iets onbekend is**: bij een eenmalige post of een jaarpost die nog twaalf maanden of verder weg ligt is `benodigdeStand` nul, dus valt er niets te delen terwijl er ook niets opgebouwd hoeft te zijn. De regel weegt daarom het **gat** mee — is er een voorkomen dat niet meer past, dan is het tekort, ook als de opbouw op peil is — en de zin komt uit `dekkingTekst()` in plaats van uit een tweede formulering. Bij een tekort staan het **percentage én het bedrag** allebei op de regel, met de noemer erbij: `graad` meet tegen wat er nu hoort te staan, `gat.gedektPct` tegen de post die niet past. De eenheid staat in een **smalle rechterkolom** (`max-width:42%`), dus hij moet kort blijven en het bedrag hoort in de zin ernaast zodra die het al noemt — een lange eenheid perst zich daar anders tot één woord per regel. Twee verhoudingen onder één woord zonder die labels is precies wat `v91` verbiedt; `gedektPct` hoort daarom in `dekking()` thuis en niet als som op het scherm. Het oordeel is een **deterministische beslisboom** in `maandOordeel()` — geen vrije formulering, geen voorspelling voorbij de cijfers, geen compliment. **Beslissing en aandacht zijn twee dingen en krijgen elk een eigen kaart**: `tekort` staat onder "Vraagt een beslissing", `let op` onder "Vraagt aandacht". De zin telt alleen de tekorten, dus samen onder één kop tellen zin en kaart verschillend en leest het scherm als een fout. Een lege kaart valt weg. Verbanden leggen mag alleen via de twee vaste regels in `maandVerband()`; verzin er geen bij en leid niets af met een heuristiek. Het leesmoment is alleen een datum (`SET.maandGelezen`): geen streak, geen teller, geen herinnering.
- **De Inzichten-hero is één kaart uit twee losse blokken** (`v135`): budgetstand en "Nog deze maand" beantwoorden dezelfde vraag — hoe sta ik er halverwege de maand voor — dus staan ze in één kaart. Bewust twee blokken die elk `''` kunnen teruggeven (`insBudgetBlok(m)` en `nogDezeMaandBody()`), niet één blok met optionele stukken: de scheidingslijn bestaat alleen als ze er allebei zijn, en zo kan de kaart nooit half leeg renderen. `insHeroKaart(m)` geeft `''` zodra er geen budget is; `renderIns()` valt dan terug op de budget-prompt van `monthStatusCard()` met `nogDezeMaandCard()` er los onder. Die keuze hoort in de beller, niet in de kaart. `nogDezeMaandCard()` is gesplitst in body en omhulsel; de koppeling met `safeToSpend().saveReserved` zit onveranderd in de body. **Inzichten is sinds `v145` de enige lezer**: dezelfde tegels stonden ook bovenaan Vooruitblik, en twee keer hetzelfde blok laat je niet zien welke je leest — de vraag "hoe sta ik er halverwege deze maand voor" hoort bij de budgetstand, niet bij een vooruitblik over maanden. `renderVooruit()` opent met de plan-zone en roept `nogDezeMaand*` niet meer aan; het omhulsel blijft alleen de terugval van `renderIns()` zonder budget. Zet de kaart niet terug op Vooruitblik, en let erop dat `openFixedDue()` en `openSafeToSpend()` daarmee alleen nog via de hero-tegels bereikbaar zijn. Er wordt hier **niets** gerekend: alle getallen komen uit `totals()`, `daysElapsed()` en die body. De status-chip uit `monthStatusCard()` staat er bewust **niet** bij: de kaart zegt hoeveel procent van de maand voorbij is en velt daar geen oordeel over. In `v166` bleek de ring-tak van `monthStatusCard()` onbereikbaar — die functie wordt alleen nog aangeroepen als `insHeroKaart()` leeg teruggaf, en dat gebeurt exact bij budget 0, waar de budget-prompt al eerder terugkeert. Alles onder die prompt (de ring, `tempo`, `ahead`, de `+8`-drempel, de dag-marker en de kv-rijen) werd elke render gebouwd en weggegooid, en is verwijderd. De kaart draagt **alle ingangen** die `monthStatusCard()` op de lopende maand had: de maandnaam opent `openBudgetCompare()`, het uitgegeven bedrag `openMonthSpend()`, het maandbudget `openPotjesVerdeling()`, en de regel "vanaf volgende maand" diezelfde met `'next'`. Ook de limiet-spiegel hoort erbij. Raak je die kwijt bij een herschikking, dan zijn drill-downs onbereikbaar zonder dat het scherm er kapot uitziet. Alles onder de hero staat onder "Verdieping", met `insVouw()` als omhulsel voor kaarten die zelf geen inklap hebben; de samenvatting in de kop komt uit dezelfde functie die de kaart vult. De maandgrafiek houdt daar zijn eigen drietraps default uit `v90` (dicht in Rustig, open in Begeleid): een sectie-indeling overrulet geen vastgelegde beslissing.
- **Potjes zijn leidend, de inkomen-limiet is een spiegel** (`v53`): `totals().budget` is de som van je potjes. `monthBudget` is referentie (`totals().limit`), geen plafond; potjes worden nooit stilletjes naar beneden geschaald. De spaar-/liquiditeitslaag leest `monthBudget`/`SET.budgets` direct en breekt dus niet als de potjes het inkomen overstijgen.
- **Maandbudget = nu-actief** (`v56`): overal het nu-actieve potjes-totaal als hoofdgetal, het geplande bedrag (`budgetsNext`) alleen als gelabelde noot.
- **Noodfonds heeft een enkele bron** (`v36`, `v38`, `v67`): het doel komt uit `noodfondsModel().doel`, de voortgang uit `spaarSaldo()`, en de mijlpaal-projectie vult op de beoogde **spaarinleg** (`monthlySavingTarget`), niet op het restsaldo. Voeg nooit een tweede formule voor hetzelfde getal toe.
- **Plan: restant zakt door** (`v98`): na de eerste verdeelronde zakt overgebleven capaciteit in prioriteitsvolgorde door naar de lopende doelen, elk tot hooguit zijn resterende behoefte. Status en `eta` worden pas na die tweede ronde bepaald. Blijft er dan nog over, dan wordt dat zichtbaar gemeld in plaats van te verdwijnen. Het noodfonds wordt bijgevuld, nooit leeggehaald. Een `auto`-doel houdt zijn betekenis (pakt wat er nog is); dat het daardoor de rest opslokt wordt uitgelegd bij het wachtende doel, met een tik naar de invoer van het doel erboven (`planAllocOpen`; aflos-items via `openPlanAlloc`).
- **Spaargeld boven het noodfonds-doel is zichtbaar, niet automatisch** (`v99`): het noodfonds-item klemt zijn voortgang op het doel en de voortgang van een spaardoel blijft het handmatige veld "Al gespaard". Geld dat daarboven op je spaarrekening staat wordt getoond (`spaarVrij()`) en is in één tik toe te wijzen aan het bovenste lopende **spaardoel** — nooit aan een aflos-item, nooit meer dan dat doel nog nodig heeft, en wat al bij doelen staat gaat eraf zodat hetzelfde geld niet twee keer als vrij verschijnt. Reken saldo-overschot nooit stilzwijgend mee in een doel.
- **De restschuld is handmatig, en dat zeggen we ook zo** (`v100`): `rest` daalt nergens automatisch mee met betalingen — het is en blijft je eigen invoer. De statusregel bij een schuld gaat daarom over de **maandbetaling** in je uitgaven, nooit over de schuldstand; het woord "aflossing" is daar bewust weg. Bijwerken loopt via `openDebtUpdate()`/`debtRestSet()`, dat alleen `rest` aanraakt en `start` meetrekt als de nieuwe stand daarboven ligt (anders wordt "afgelost" negatief). Afboeken stopt op een vloer: de slottermijn bij `financiallease`, anders nul. Introduceer hier geen automatische schatting.
- **Het noodfonds-doel mag ook een getal van jezelf zijn** (`v101`): `noodfondsModel()` blijft de enige bron, maar kent nu naast `doelAuto` (essCrisis × maanden) een `doelVast` uit `SET.nfDoelVast`; `doel` is de vaste waarde als die er is, anders de schatting. Alle lezers (hero, plan-item, mijlpaal, Instellingen) hangen aan `doel` en volgen dus vanzelf — voeg geen tweede formule toe. Leeg, nul, negatief of onzin telt als niet gezet, zodat er nooit een doel van nul ontstaat met een onbruikbaar percentage. Default blijft de schatting: nietsdoen verandert niets, de override staat zichtbaar in het noodfonds-paneel en is in één tik terug te draaien. Waar het doel vaststaat mag geen tekst meer "N mnd" beweren.
- **Dezelfde euro mag nooit bij twee doelen staan** (`v102`): de voortgang van het noodfonds wordt telkens herrekend uit je saldo (`planMap`: `min(saved, doel)`), terwijl "Al gespaard" van een spaardoel handmatige invoer is. Gaat het noodfonds-doel omhoog nadat je vrij spaargeld aan een doel gaf, dan claimen ze samen meer dan er staat. `spaarOver()` meet dat verschil, `spaarOverLine()` meldt het bij het noodfonds-item en `spaarOverAf()` haalt het weg — van de laagste prioriteit naar boven, spiegelbeeld van `spaarVrijToe()`, dat juist aan het bovenste doel geeft. Verlaag nooit automatisch: de app meldt en de gebruiker tikt. Het noodfonds zelf wordt hierbij niet aangeraakt, en bij onbekende saldi volgt geen melding.
- **Een kerncijfer staat altijd in verhouding tot zijn doel** (`v103`): `kpiAfstand()` rekent het verschil met de band uit in procentpunten én in euro's, uit dezelfde `totals(m)`/`splitFixedVar(m)` waar het percentage vandaan komt — nooit een eigen som, anders lopen bedrag en percentage uit elkaar. `dir` bepaalt het teken (bij sparen is meer goed, bij een drukmetriek niet) en `kpiAfstandTxt()` kiest de woorden per metriek. Budgetnaleving meet tegen je potjes, de rest tegen je inkomen (`v53`). Een **lopende maand krijgt geen afstand op de tegel**: een halve maand leest anders als prestatie; in de sheet staat hij wél, met de tussenstand erbij. Geen basis betekent geen bedrag (`v59`/`v73`). De doellijn in `miniSparkLine()` wordt bij een doel buiten bereik op de rand geklemd in plaats van weggelaten — wie ver van zijn doel staat heeft de referentie juist nodig.
- **De transactielijst achter een kerncijfer telt op tot dat cijfer** (`v104`): `kpiTx(key,m)` selecteert met exact dezelfde predicaten als `totals()` en `splitFixedVar()` (`isExpenseTx`, `isFixed`), zodat de lijst per definitie klopt met het getal erboven. Bouw hier nooit een tweede filter: een lijst die iets anders zegt dan het cijfer is erger dan geen lijst. Vast en variabel zijn samen precies de budget-lijst — geen overlap, geen gat — en doorstroomposten vallen er automatisch buiten (`v77`). Het **inkomen** in de bespaarquote komt uit `totals()` en is met `autoIncome` uit je eigen instelling, niet de som van je inkomsten-transacties; het staat daarom als aparte regel met zijn bron (`KPI_INCBRON`) in plaats van tussen de transacties.
- **De bijdrage per transactie deelt de noemer van het cijfer** (`v105`): `kpiBijdrage(key,t,basis)` gebruikt `kpiAfstand().basis` — je potjes bij budgetnaleving, anders je inkomen — zodat de punten optellen tot het getal erboven: recht voor de drukmetrieken, en `100 + som` voor de bespaarquote. Het teken volgt de betekenis via `dir`: een uitgave duwt je bespaarquote omlaag en een drukmetriek omhoog, en een terugstorting draait dat vanzelf om. De lijst staat op gewicht, niet op datum; bij gelijk gewicht wint de nieuwste. Één uitleg boven de lijst via `jrg('procentpunt')`, nooit per regel.
- **Een verschuiving splitst in teller en noemer** (`v106`): elk kerncijfer is teken × X/B, en `kpiDelta()` splitst de verandering exact in een **teller-effect** `(X1−X0)/B1` (wat je anders uitgaf, en dat splitst verder per categorie) en een **noemer-effect** `X0·(1/B1−1/B0)` (wat je inkomen of budget deed). Samen zijn ze de hele verschuiving. Sla die tweedeling nooit over: zonder het noemer-effect leest een gedaald inkomen als "je gaf meer uit", en dat is onwaar. `kpiCatSom()` clampt bewust niet zoals `catSpendMap()`, anders telt een netto-negatieve categorie niet mee en klopt de optelling niet meer. Bij een lopende maand volgt geen vergelijking: een halve maand tegen een hele zegt niets.
- **Het noodfonds schat op afgeronde maanden** (`v107`): `noodfondsModel()` rekent over `months().filter(m => m < de lopende maand).slice(-12)`. `months()` telt de lopende maand altijd mee en die is niet af; bij `NF_PERIODIEK` (verzekering, belasting) wordt het **gemiddelde** genomen, dus een halve maand drukte het doel omlaag en liet het binnen de maand vanzelf oplopen. Is er nog geen afgeronde maand, dan blijft de lopende de enige bron (`winLopend`) — anders levert een verse import geen doel op — en de sheet zegt er dan bij dat het nog verschuift.

### FIRE-laag
- **`fireInputs()` is de enige naad** (`v32`): laag A leest Minder, laag B (`fireState`/`fireModel`/`fireMonteCarlo`) is puur, laag C rendert. Laat laag B nooit direct uit Minder-state lezen.
- **Bezittingen, schuld en vermogen apart** (`v44`, `v49`): belegd groeit, cash is vlak, schuld amortiseert. De `grow`-vlag per bezitting wordt gerespecteerd (niet-groeiend gaat naar het vlakke potje). Harde invariant, vastgelegd in een test: `netto === bezittingen - schuld` op elk punt, ook in de Monte Carlo.
- **Een scenario grijpt aan op de naad, nooit in de projectie** (`v124`): `reisModel(ovr)` neemt een optionele override (`{vermogenDelta}`) en past die toe op een **kopie** van het inputs-object, tussen laag A en laag B in. `fireInputs()` blijft de enige plek die Minder leest en `fireState`/`fireModel` blijven puur (`v32`); zonder argument gaat exact hetzelfde object door als voorheen, dus dan is er niets veranderd. De delta landt op `belegd` — alleen die laag compoundeert — met `netWorth` mee voor wat er daadwerkelijk af kon en `belegdItems` proportioneel geschaald, anders tellen de aandelen in `assetParts` op tot meer dan één. Uitgaven en inleg blijven ongemoeid: geld opnemen verandert die niet. `onttrekkingKosten(bedrag)` draait het model twee keer en geeft `{maandenLater, jaarNu, jaarNa, bedrag}`, of `null` zodra er iets ontbreekt. Let op: **`M.missing` is een object** `{balances, assets}` en dus altijd truthy — toets de velden, niet het object. De jaartallen komen uit de bestaande FIRE-mijlpaal (één bron), het maandverschil uit `fireKruisMaanden()`, dat het kruispunt lineair interpoleert binnen het jaar; `reach()` werkt op hele jaren en zou twee scenario's altijd een veelvoud van 12 maanden uit elkaar zetten.
- **Belasting loopt via `fireBelasting()`** (`v35`): de enige plek voor vermogensheffing, opschaalbaar naar Box 3. De default is een zichtbare plaatshouder, gelabeld als benadering en uitdrukkelijk geen fiscaal advies.

### Import en data
- **PSD2 soft-dedup ontdubbelt alleen over bronnen** (`v51`): `if(ex && ex.acc!==t.acc && ex.src!==t.src) continue;`. Twee live rekeningen met dezelfde bron (N26 Main en Spaces) mogen elkaar nooit wegvegen.

### Eerlijkheid: onbekend blijft onbekend
- **Geen bedrag zonder saldo** (`v59`): bij `totalBalance().known===0` toont de hero `onbekend` met een CTA naar de saldo-invoer, en de drill-down spreekt de hero niet tegen. `safeToSpend()` rekent onveranderd door; dit is puur presentatie.
- **Geen alarm op wat we niet weten** (`v73`): het saldo-alarm vuurt vanaf een enkel bekend saldo en vermeldt waar het op rekent. Bij nul bekende saldi volgt hooguit een rustige eenmalige uitnodiging, geen alarm.

### Taal en toon
- **`FIRE_VOICE` naast `COACH_SPEC` en `MECHANISM_SPEC`** (`v34`): op het FIRE-scherm kalm, band in plaats van belofte, geen advies en geen urgentie.
- **Een term per begrip** (`v91`): "spaarinleg", niet "spaarruimte". "restsaldo" is een eigen begrip (inkomen min uitgaven) en blijft bestaan. Uitleg loopt uitsluitend via het bestaande `jrg()` + `JARGON` + `#tipPop`-mechanisme; bouw er geen tweede naast.
- **`dir` zegt welke kant beter is, `sign`/`base` hoe de teller in het cijfer landt** (`v109`): bij de eerste vier kerncijfers vielen die samen, bij de spaarquote niet meer — daar telt een storting juist pósitief terwijl hoger ook beter is. Elk cijfer is nu `base + sign × X/B × 100`, met `kpiTxWaarde()` als enige plek waar de per-transactie-waarde bepaald wordt (uitgave `-amount`, spaarinleg `+amount`). Leid het teken nooit meer af uit `dir`. De spaarquote heeft een eigen bron `savedNet()`: zelfde meting als `savedThisMonth()` maar zonder de clamp op ≥ 0, want ontsparen moet zichtbaar zijn; zonder gemarkeerde spaarrekening valt hij terug op de categorie `sparen` en zonder enige spaarboeking blijft hij `null`.
- **Getalnotatie** (`v75`, `v76`): NL-notatie, minteken voor het euroteken (`-€128,00`), nul-guard tegen `-€0,00`, en `euroK()` als enige compacte vorm. `euro`, `euro0` en `euroK` volgen altijd dezelfde conventie.

### Visuele taal
- **Alles via tokens in `:root`** (`v75`, `v83`, `v96`): kleuren, spacing, radius en de type-schaal (`--fs-xs` t/m `--fs-xl`). Geen hardgecodeerde hex, ook niet in inline-SVG (die gebruikt `var()` in presentatie-attributen).
- **Status zit in het label, niet in de kleur** (`v78`, `v93`): een neutrale datakleur (`--bar`), geen gridlijnen, en amber uitsluitend voor echte aandacht (over budget, tekort, verstreken terugbetaaldatum). Informatieve signalen dragen `--mut`/`--mut2`. De ring is gereserveerd voor de hero.
- **Data-paletten zijn identiteit, geen status** (`v83`): categoriekleuren, `DEBTCOL` en `ASSETCOL` houden hun eigen kleur en volgen het thema niet.
- **Thema's** (`v83`, `v97`): de donkere look blijft de default (leeg `SET.theme`). Nieuwe kleuren in het Privé-thema worden nagerekend tegen WCAG AA (tekst 4,5:1 op zowel `--bg` als `--card`), niet op het oog beoordeeld.

- **Een instellingsregel klapt uit, tenzij hij een eigen sheet heeft** (`v137`): de `cats`-tabel in `renderSet()` kent een optioneel zevende veld met een sheet-aanroep. Staat dat er, dan opent de regel die sheet en klapt hij nooit inline uit; staat het er niet, dan geldt `toggleSet(id)` zoals altijd. Dat veld dragen precies de regels waarvan de sheet **ook ergens anders** de ingang is: budget (ook vanuit Inzichten) en de coach-avatar (ook vanuit de coachkop op `s-act`). Eén oppervlak per editor, meerdere ingangen (`v61`). Bouw daar geen `id===`-uitzondering meer naast.
- **Wie vanaf twee plekken te wisselen is, tekent het hele scherm opnieuw** (`v137`): `setCoachTone()` en `setCoachAvatar()` riepen `renderActions()` aan en gingen er dus van uit dat je op de Coach-tab stond. Nu loopt dat via `coachHerteken()` — `render()`, of `renderSet()` als er nog geen transacties zijn, want dan valt `render()` terug op `renderEmpty()` en zou de instellingsregel blijven staan. `openCoachAvatar()` maakt `_setSheet`/`_nfSheet`/`_budgetSheet` leeg, net als `openBudgetEditor()`: zonder dat tekent de `render()` erna een eerder geopende sheet over de coach-sheet heen. De namen van de avatars staan in `COACH_AV_NAAM` en de toon in `COACH_TOON_NAAM`, zodat de sheet en de samenvatting op de instellingsregel (`coachSamenvatting()`) niet uiteen kunnen lopen. De `.coachhead` op `s-act` blijft voorlopig staan: beide ingangen werken naast elkaar tot dat scherm wordt opgeheven.

- **Het coachgesprek draait in de sheet en start via `coStart()`** (`v138`): de draad zat in `s-act` en was daarmee aan dat scherm vastgeklonken. `coSheetShell()` zet dezelfde twee containers (`#coThr`, `#coCh`, dezelfde klassen) nu in `#sheet`, en `coStart(onderwerp, m)` is de enige ingang: hij breekt een lopend gesprek af, claimt het sheet-oppervlak en roept `coOpen(m)` aan, dat zelf `coReset()` doet. `coTopic()` kan die rol niet dragen — die wordt van binnenuit een lopend gesprek aangeroepen, dus openen en resetten zou elke onderwerpwissel tot een herstart maken. Het gesprek start alleen nog als je Coach echt opent (`go('act')` zet `_coReopen`), **niet bij elke render**: de oude `coOpen(m)` aan het eind van `renderActions()` zou de sheet laten openklappen terwijl je op een ander scherm staat. Onderwerpen staan in `CO_ONDERWERPEN`; een onbekend onderwerp valt terug op het gewone gesprek in plaats van een half pad te openen.
- **Sluiten is afbreken, en afbreken schrijft nooit** (`v138`): `closeSheet()` roept `coAfbreken()` aan, dat `_coGen` ophoogt en `coEnd()` doet. Die ophoging is wat een nog lopende `await coSay()` stopt: elke stap toetst `_g!==_coGen` en `coSay()` controleert bovendien `document.body.contains(row)`. `coAfbreken()` legt **niets** vast en haalt **niets** uit `SET.coachLog`. `coCommit()` en `coAfspraakOpen()` mogen uitsluitend vanuit een lopend gesprek worden aangeroepen; zij zijn de enige plek waar een afspraak ontstaat. Let op de volgorde in `coAfspraakOpen()`: de keuze 'Afspraak aanpassen' wist de afspraak van deze maand vóór het vervolggesprek begint, dus breek je daarna af, dan is de oude weg zonder dat er een nieuwe staat. In `coShowAction()` staan het wissen en het schrijven wel direct naast elkaar zonder `await` ertussen, dus dat pad kan niet halverwege breken.
- **Het invoerscherm parkeert de draad, het overschrijft hem niet** (`v138`): `coTextSheet()` gebruikt dezelfde sheet als het gesprek. `coParkeerDraad()` haalt `#coThr` en `#coCh` er als **dezelfde nodes** uit en `coHerstelDraad()` hangt ze terug, zodat `_coT`/`_coC` geldig blijven en de draad het invoerscherm overleeft. Herbouw ze nooit uit HTML: dan raak je de nodes kwijt waar de bouwstenen naar wijzen. `coTextSave()` herstelt in plaats van te sluiten; buiten een gesprek valt hij terug op `closeSheet()` zoals vanouds. Het invoerscherm wegtikken breekt het gesprek af, zonder afspraak.

- **Vier regels uit `coachItems()` wonen nu in de signalen-engine** (`v143`, deel A): het coachscherm `s-act` wordt opgeheven, en `coachItems()` bevatte vijf regels die nergens anders in de app bestonden. Vier zijn verhuisd naar `scoreNotifs()` met dezelfde drempels, teksten en acties: `savrules` (spaarstortingen tellen als uitgave), `meevaller`, `inflatie` en `overstreak`. Wat de signaal-vorm erbij vraagt en `coachItems()` niet had zijn een icoon en een score; die staan bewust onder de acute meldingen. De knoplabels vervallen, want een meldingrij heeft geen knop en elk label herhaalde de zin uit `context` al. **Regel 5** (grootste variabele hefboom) is **niet** verhuisd: `coachRuleOptions()` doet hetzelfde en voedt al drie gespreksonderwerpen. De fallback "Je zit op koers" is evenmin verhuisd: de positieve noot `room` zegt dat al. `coachItems()` blijft in deel A staan, want `renderBehavior()` leest hem nog. `coachFocus()` en `nextTip()` zijn in `v165` verwijderd: alle vijf regels hebben een thuis — vier in `scoreNotifs()` en regel 5 in `coachRuleOptions()` — dus er viel niets meer te vergelijken. **Twee acties overleven deel B niet ongewijzigd** en vragen dan een beslissing: `toggleStep('savings')` en `toggleActDeep()` sturen allebei `renderActions()` aan.

- **De afspraaklus raadt nooit** (`v142`): `vorigeAfspraak()` pakt de nieuwste `afspraak`-entry uit **precies** de voorgaande kalendermaand, niet verder terug. `afspraakUitkomst()` geeft alleen een oordeel als de afspraak een **categorie** draagt; dan vergelijkt hij `catSpendMap()` van de maand vóór de afspraakmaand met die van de afspraakmaand zelf. **Twee afgeronde maanden, bewust niet de lopende**: een volle maand tegen een halve zetten laat elke categorie dalen en zou stelselmatig 'gelukt' melden (`v107`/`v113`). Een daling telt pas boven `AFSPRAAK_MARGE` (2%), één constante. **`niet te toetsen` is het hoofdpad, geen foutgeval**: geen van de vijf functies achter de maandregels is maand-gebonden (`bufferMaanden()`, `dekking(12)`, `spaarVrij()`, `maandDoel()`, `maandPatroon()` beschrijven de stand van nu), dus een afspraak met alleen een regelKey is niet te meten. Bouw daar geen tweede meetmethode voor en leid **nooit** iets af uit de afspraaktekst: geen trefwoorden, geen aanname. Dan vraagt de app het je zelf, en dat antwoord landt als `{type:'reflectie', text, af:<ts van de afspraak>}`. Eén keer per maand via `SET.afspraakGezien={ts,ym}`, dezelfde vorm als `coachRecall`; de regel valt ook weg zodra de afspraak van deze maand er staat. Geen streak, geen teller, geen score.

- **Eén route naar een afspraak, en de oude verdwijnt pas als de nieuwe er staat** (`v141`): `coAfspraakSchrijf(txt, extra)` is de enige plek waar een afspraak ontstaat. Wissen en schrijven staan er direct naast elkaar **zonder `await` ertussen**, dus dit pad kan niet halverwege afbreken. `coCommit()` en `coShowAction()` lopen er allebei doorheen. `coAfspraakOpen()` wist daarom **niets meer vooraf**: brak het gesprek af na de tik "Afspraak aanpassen", dan was je oude afspraak weg zonder dat er een nieuwe stond, en dat merk je pas een maand later. `extra` draagt `regel` (de regelKey van het maandscherm) en waar van toepassing `cat`, zodat de afspraak over een maand leesbaar is zonder de context van het gesprek. Voeg hier geen tweede schrijfroute naast.
- **Het maandgesprek opent bij één regel, niet bij een samenvatting** (`v141`): `coTopicMaand(m, regelKey)` leest die ene regel uit `maandRegels()` en opent met `r.gevolg`, exact de zin die het maandscherm toont. `dekking`, `doel` en `patroon` krijgen `coachRuleOptions(m)` (alleen regels die uit staan, want `toggleVooruitCut()` schakelt om), `buffer` en `aansluiting` krijgen de actie die al achter die rij hangt. **Een coachRule is geen afspraak**: die keuze schrijft `{type:'tip'}` en gaat daarna pas naar `coMaandBevestig()`, de enige stap die een afspraak maakt. Zonder die tik is er geen afspraak. `maandCoachIngang()` kiest de zwaarste regel via `coMaandZwaarste()` — tekort boven let op, daarbinnen `MAAND_VOLGORDE` — en toont een bestaande afspraak van deze maand **in plaats van** de ingang: één afspraak per maand, dus nooit twee wegen naar een nieuwe. Alleen onbekende regels betekent geen ingang; alles ok geeft een rustige zin naar `algemeen`, zonder felicitatie.
- **Elk onderwerp verklaart zelf of het een afspraak mag schrijven** (`v140`): `CO_ONDERWERPEN` is een tabel `{onderwerp:{afspraak:bool}}` en `coMagAfspraak()` leest die. Een onderwerp dat erbij komt moet die keuze dus maken in plaats van hem te erven; een onbekend onderwerp staat er niet in en gedraagt zich als voorheen. **`coEnd()` wist `_coOnderwerp` bewust niet** — een weigering in `coAfspraakOpen()`/`coShowAction()` zou anders het onderwerp loslaten en de volgende poging weer toelaten, dus die twee weigeren met een kale `return`. `coAfbreken()` wist hem wel, en `coStart()` zet hem bij elk nieuw gesprek.
- **Het onderwerp `horizon` gaat over gevolg, en verzint geen precisie** (`v140`): `coHorizonBron()` is de enige bron en kiest in volgorde — een doel met streefdatum en een gat uit `maandDoel()`/`doelTempo()`, anders het verschil tussen het bereikjaar van de FIRE-mijlpaal en `M.targetYear`. Dat tweede geval telt alleen als de gebruiker het jaar zelf bepaalde (`M.R.override` of een geboortejaar): anders valt `targetYear` terug op nu + 30 jaar en zou het gesprek een jaartal noemen dat niemand koos. `M.missing` is een object en dus altijd truthy (`v124`), dus getoetst wordt `M.missing.balances` — een onbekend saldo diskwalificeert de projectie (`v59`/`v73`), géén bezittingen is geen onbekende maar een lege laag. `coTopicHorizon()` opent met `doelTempoLine(p)` of met de twee jaartallen plus "Een band, geen belofte", biedt `coachRuleOptions(m)` aan en zet om met `toggleVooruitCut()`. Alleen regels die **nu uit staan** worden aangeboden: `toggleVooruitCut()` schakelt om, dus een actieve regel zou juist uitgaan. Onttrekkingen horen hier niet; `onttrekkingKosten()` heeft zijn eigen ingang op het vermogensscherm.
- **De ingang van `horizon` staat in het plan-blok, niet bij de besparingschips** (`v140`): die chips bestaan niet meer op het scherm. `vooruitForecast()` en `goalCoachCard()` vielen in `v80` uit de samenstelling en zijn sindsdien dode code; ze bouwen `.vh-hef`/`.gc-tip` nog wel, maar niemand roept ze aan. `coHorizonVraag()` hangt daarom op planniveau in `renderPlan()`, naast `vrijLine` en `spaarVrijLine()` (`v126`), tussen de doelen waar de streefdatum en het tempo al staan. Het plan-blok is in Begeleid ingeklapt, dus de vraag staat achter één tik op "Mijn plan". Let op bij het verwijderen van dode code: `financeModel()` leest `SET.coachRules` wél (de projectie schuift dus meteen mee), maar geen enkel blok op de Vooruitblik rendert die regels nog.
- **Een terloops gesprek raakt de maandafspraak nooit** (`v139`): het onderwerp `lek` begint vanaf Inzichten bij de bevinding zelf en is dus geen maandgesprek. `coachThisMonthAfspraak()` laat één afspraak per maand toe en `coAfspraakOpen()` wist de bestaande vóór hij een nieuwe schrijft, dus een gesprek dat er terloops bij begint mag daar niet aan komen. `coMagAfspraak()` is de enige toets (`!(window._coLive && window._coOnderwerp==='lek')`) en staat op **drie** plekken: `coachLogAdd()` weigert een entry van het type `afspraak` (de enige schrijfroute), en `coAfspraakOpen()` en `coShowAction()` stoppen vóór hun `filter` (de twee die eerst wissen). Die vlag hoort op het gesprek en niet in elk pad afzonderlijk: zo kan een pad dat er later bijkomt hem niet omzeilen. `coEnd()` laat `_coOnderwerp` weer los. Het onderwerp `lek` schrijft hooguit `{type:'tip'}`, net als `coTopicTips`.
- **Het onderwerp `lek` rekent niets en verzint niets** (`v139`): `coTopicLek()` opent met `coachWeekRisk(m).txt` (de kant-en-klare zin met bedrag en categorie, dus geen eigen openingstekst en geen groet), biedt `coachRuleOptions(m)` aan als keuzes en zet een keuze om met `toggleVooruitCut(key,cut)` — dezelfde omzetting als op de vooruitblik. Afsluiten gaat met de bestaande slotzin plus `coTone('slot')`, zoals `coShowAction()` dat doet. Schrijf hier geen eigen tekst waar een bestaande functie er al een levert. De ingang is `insLekVraag(m)`, direct onder `whatStandsOutLine(m)` in `renderIns()`, en rendert `''` zodra `coachWeekRisk()` geen `warn` geeft of `coachLeak()` niets oplevert: geen lek, geen regel, geen lege staat. Eén ingang, alleen op Inzichten.

### Modus
- **Rustig toont minder, rekent nooit anders** (`v20`, `v90`): default is `begeleid`, de keuze is altijd omkeerbaar. Inklap-vlaggen zijn drietraps via `collapOpen()`: geen keuze betekent dat de modus beslist, een expliciete keuze van de gebruiker wint altijd.

## Testconventie
Elke wijziging: `check.js` groen, de Playwright-harness in `tests/` groen, en een nieuwe `tests/<onderwerp>.spec.js` voor elke nieuwe regel of invariant. Meet layout op 360 en 390px. Raakt de wijziging de cache of `ASSETS`, hoog dan `CACHE` in `sw.js` op.

## Changelog
De volledige changelog staat in `CHANGELOG.md` (niet automatisch geladen). Lees dat bestand alleen als je de geschiedenis van een specifieke wijziging nodig hebt.
